# Fly Pixel Game — передача для підключення мозку (Claude Code)

## 🧭 Почни звідси — огляд для нового чату (стан на 2026-09-15)

> ℹ️ **Стан на 2026-09-21: повний ~139k-нейронний конектом — тепер
> основна (production) версія** (`main` на GitHub/Vercel; ця директорія —
> worktree гілки `full-connectome`, що дорівнює `main`, див. `CLAUDE.md`).
> Старий 70-груповий рушій лишився лише в git-історії (до коміту
> `09aebfe`) і як автоматичний fallback у `js/brain-worker-bridge.js`.
> Огляд одразу нижче описує ЗАГАЛЬНУ базу (70-групову модель) — вона
> й досі правдива (той самий код), але Web Worker з повним конектомом
> МІНЯЄ "як запустити" (нижче) і скасовує пункт "окремий проєкт, не в
> цьому репозиторії" з дорожньої карти й "Плани" в README.md. Деталі,
> статус і живі перевірені цифри — розділ "🧪 Full-connectome
> experiment" нижче, він завжди найактуальніший.

**Що це, в двох реченнях.** Один HTML-файл (`fly-pixel-game.html`,
~4.8МБ, усі спрайти вшиті base64) — 2D pixel-art симуляція мухи на
робочому столі. Усі рішення мухи (куди йти, коли їсти/чиститись/летіти/
боятись/навчатись уникати небезпеку) виходять із СПРАВЖНЬОГО нейронного
конектома (клон [`snedea/flybrain`](https://github.com/snedea/flybrain),
~70 лумпованих груп нейронів БАЗОВО, а в цій гілці — повний ~139k-
нейронний FlyWire конектом поверх, див. попередження вище), а не з
`Math.random()` — щодо `Math.random()`, що лишився, дивись нижче "Що НЕ
реалізовано" й самі коментарі `[BRAIN]` у коді.

**Як запустити (У ЦІЙ ГІЛЦІ, з full connectome — інакше, ніж у README.md
й ніж написано нижче в "Стратегія на майбутнє"/"Плани").** `file://`
(відкрити файл напряму подвійним кліком) БІЛЬШЕ НЕ ПРАЦЮЄ для повного
конектома тут: `fetch('data/connectome.bin.gz')` і `new
Worker('js/sim-worker.js')` браузер блокує під origin `file://` —
конектом мовчки НЕ завантажиться, гра тихо відкотиться на легасі
70-групову модель (`js/brain-worker-bridge.js` сам ловить цю помилку й
підставляє fallback, консоль покаже warn, а рядок статусу під кнопками —
"59 neuron groups — FlyWire approximation (fallback)"). Обов'язково
потрібен HTTP(S)-сервер:
```bash
python3 -m http.server 8791   # порт довільний, збігається з .claude/launch.json тут
# → http://localhost:8791/fly-pixel-game.html
```
(Для звичайного `main`/`../fly-brain-handoff` без цієї гілки — там
`file://` і далі працює як задокументовано в README.md, бо там worker/
`data/`/`js/` немає.)

**Файли репозиторію:**
- `fly-pixel-game.html` — уся гра, єдиний файл, що деплоїться
- `assets/*.png` — вихідні спрайт-аркуші (довідково, у рантаймі не читаються)
- `README.md` — коротко для GitHub-відвідувача, як запустити/задеплоїти
- `vercel.json` — маршрутизація деплою (`/` → `fly-pixel-game.html`)
- `HANDOFF.md` (цей файл) — стан проєкту, архітектура, дорожня карта
  (чекліст, що реалізовано/заплановано)
- `CHANGELOG.md` — повна хронологія розробки, сесія за сесією: кожен
  знайдений баг, кожне рішення з обґрунтуванням і перевіркою. Туди йти,
  коли потрібні деталі "чому саме так", а не просто "що є зараз".

**Архітектура за 30 секунд:** три шари — мозок (конектом, `BRAIN.*`) →
тіло (анімації/фізика/колізії) → сцена (фон/зони/сутності). Технічні
деталі конектома — нижче; повна хронологія кожного архітектурного
рішення — `CHANGELOG.md`.

**Методологія тестування, яку варто знати перед будь-якими змінами:**
змін перевіряються headless-браузером (не лише читанням коду) — драйвиться
`loop(t)` синтетичним годинником напряму з консолі/JS-виконання, а НЕ
справжнім `requestAnimationFrame`. ВАЖЛИВО: якщо повторно тестуєш на вже
завантаженій сторінці (не перезавантажуючи), обов'язково скинь
`lastHungerTick`/`lastBrainTick`/`nextPoopAt` до свіжого `tNow` на
початку кожного нового прогону — інакше застарілі таймери з попереднього
прогону дають катастрофічно неправильні результати (задокументовано як
знайдений і вирішений артефакт методології в `CHANGELOG.md`).

**Поточний фокус (2026-09-15):** підготовка до деплою на GitHub + Vercel
(мобільна адаптація вже додана в код; `vercel.json`/`README.md`/
`.gitignore` підготовлені; git-репозиторій і сам деплой — наступний крок,
див. "Деплой" у дорожній карті нижче).

**Стратегія на майбутнє (початковий план, сказаний користувачем — ЧАСТКОВО
ЗАСТАРІЛО, лишено для історії):** спочатку йшлося про "легку" 70-групову
версію як постійну продакшн, і повну ~130k+-нейронну модель як окремий
паралельний проєкт/хостинг, що не замінює цю версію. **З 2026-09-16 це
вже не так** — повна модель тепер активно розробляється ПРЯМО ТУТ, у
git worktree гілки `full-connectome` (не окремий репозиторій), з чіткою
метою або замінити 70-групову версію в `main` (якщо спрацює), або бути
видаленою (якщо ні) — див. `CLAUDE.md` "Outcome plan" і розділ "🧪
Full-connectome experiment" нижче для актуального плану.

## 🗺️ Дорожня карта / історія фіч

Чекліст — `[x]` реалізовано (деталі й обґрунтування — шукай відповідний
заголовок у `CHANGELOG.md`), `[ ]` заплановано/не почато.

**Ядро / мозок**
- [x] snedea/flybrain ~70-групового конектома підключено (`dendriteAccumulate`
  → `fireNeuron` → `runconnectome`, драйви hunger/fear/fatigue/curiosity/groom)
- [x] Усі первісні точки `[BRAIN]` (рандом/таймери) замінені реальними нейронами
- [x] Статус-рядок показує реальні нейрони, що спрацювали цього такту
  (`BRAIN.firedThisTick`), а не текстовий опис дії
- [x] Повна ~139k-нейронна модель (FlyWire) через Web Worker — **у ЦІЙ
  гілці** (`full-connectome`, не окремий репозиторій), Фаза 0+1
  підтверджені 2026-09-16, див. "🧪 Full-connectome experiment" нижче
  (у гілці `main`/`../fly-brain-handoff` цього немає — там і далі чисто
  70-групова легасі модель)
- [x] Реал-тайм 3D-візуалізація активності нейронів — зроблено 2026-09-16
  (Three.js + реальні координати FlyWire, кнопка "3D мозок", див. розділ
  "✅ 3D-візуалізація живої активації конектома" нижче); зв'язків
  (ребер-з'єднань) поки НЕ візуалізовано, лише самі нейрони-точки

**Фізіологія**
- [x] Голод: фазова модель (харчова кома → перетравлення → гострий голод)
- [x] Дофамін / стрес: 0-100 з експоненційним згасанням
- [x] Втома польоту / відновлення на землі, гравітація змушує сісти на нулі
- [x] Фототаксис: два реальних джерела світла різної сили тяжіння
- [x] Повна термальна карта сцени (кожен об'єкт окремо, день/ніч)
- [x] Спрага — зроблено 2026-09-17 (окремий бар "Спрага: %", кавова пляма
  тепер ПЕРШ ЗА ВСЕ джерело води — велике полегшення спраги, мале голоду;
  див. розділ нижче про межу датасету — немає реальної `DRIVE_THIRST`-групи)

**Сцена й фізика**
- [x] Растрова карта зон (walk/hot/flight/floor/pcside/vertical) за кольором
- [x] Вільне повзання між суміжними наземними/лазильними зонами без зльоту
- [x] Лазіння по вертикальних поверхнях
- [x] Автономний зліт/приземлення (без кнопок), реальна гравітація
- [x] День/ніч перемикач, що реально впливає на термальну карту й світло

**Сутності на столі**
- [x] Їжа (салямі/яблуко): запах → переслідування → поїдання →
  `GUS_GRN_SWEET → MB_DAN_REW` (реальна дофамінова нагорода)
- [x] Какульки (`poop`)
- [x] Тарган: без мозку/AI, чисто випадковий рух, з'являється після
  першого "Кинути їжу" з затримкою
- [x] Тарган → зір/дотик/втеча мухи: `VIS_LO`/`VIS_LC`/`DN_STARTLE`/
  реально задіяний дотиковий шлях `BRAIN.stimulate.touch`
- [x] Оголений провід: візуальне розміщення (color-landmark matching) +
  реальна больова логіка (`NOCI`, SEE/TOUCH тіри, форсований зліт)
- [x] Кавова пляма: з'являється при струсі столу, вода+кофеїн,
  `GUS_GRN_WATER` вперше реально активний

**Навчання (дофамін-гейтована асоціативна пластичність)**
- [x] Чашка: `MB_KC_HOT` + `THERMO_MUG → MB_DAN_PUN`, вага
  `MB_KC_HOT→MB_MBON_AV` реально росте в рантаймі
- [x] Провід: `MB_KC_WIRE` + `VIS_WIRE`/`NOCI → MB_DAN_PUN`, з нуля
  (жодної вродженої аверсії, класична павловська схема)
- [x] Кофеїн прискорює навчання (`×1.5` на `MB_PLASTICITY_RATE`, поки діє)
- [ ] Тарган (`MB_KC_ROACH`) — навмисно ОСТАННІЙ у черзі; користувач
  прямо просив нагадувати про це, якщо він сам забуде й попросить раніше
- [ ] Розділення джерел `VIS_LC` (зараз стіна/тарган/клік — спільний
  нейрон) — обов'язкова передумова для навчання таргана, інакше страх
  хибно узагальниться на звичайні стіни

**Деплой (GitHub + Vercel, у процесі)**
- [x] Мобільна адаптація: `viewport` meta, responsive CSS (canvas
  масштабується під ширину екрана, внутрішня роздільність гри 380×680
  не змінюється — координати кліку/тапу вже й так перераховувались
  через `canvas.width/rect.width`, тому нічого не зламалось)
- [x] `vercel.json` (маршрут `/` → `fly-pixel-game.html`), `README.md`,
  `.gitignore` підготовлені
- [x] `git init` + перший комміт, GitHub-репозиторій підключено
  (`github.com/vitaliidudka/FlyBrainGame`), Vercel імпортовано
- [x] Мобільний фікс: `canvas { touch-action:none }` блокував свайп-скрол
  до контролів/індикаторів нижче канвасу (на мобільному він займає майже
  весь екран, тож свайп по ньому браузер повністю ігнорував) — змінено на
  `touch-action:pan-y`: вертикальний скрол тепер проходить, pinch-zoom і
  горизонтальний пан і далі заблоковані; повноцінна мобільна верстка
  (щоб усе влізало без скролу) — окремий майбутній крок
- [x] Виправлено email автора комітів: перші 2 коміти пішли з
  автозгенерованого `vitaliidudka@Vitaliis-Laptop.local` (git не мав
  налаштованого `user.email`) — Vercel заблокував деплой повідомленням
  "commit author email is not valid, ensure your git email matches your
  GitHub account". Виправлено `git config user.email/user.name` ЛОКАЛЬНО
  в цьому репозиторії (не глобально) на реальний email користувача; ЦЕЙ
  комміт — перший із правильним автором. Історія НЕ переписувалась
  (`git commit --amend`/rebase + force-push на `main` свідомо не робив —
  це деструктивна операція, яку не варто виконувати без прямого прохання
  саме на цю дію) — якщо Vercel досі блокує деплой через СТАРІ коміти в
  історії (а не лише останній), доведеться або переписати історію
  (force-push), або видалити й перестворити репозиторій наново.

## 🧪 Full-connectome experiment — стан дослідження (2026-09-16)

**Це — саме ТА гілка**, де реалізується "окремий майбутній проєкт" з
розділу нижче. Дослідив upstream `snedea/flybrain` (реальний GitHub-репо,
не наші файли) ПЕРЕД тим, як щось писати — нижче факти, а не здогад.

**Реальні цифри повного конектома** (`data/neuron_meta.json` у upstream):
139 255 нейронів, 2 698 236 зв'язків, 63 функціональні групи (та сама
таксономія назв, що й у нашій ~70-групповій моделі — `VIS_LC`, `MB_KC`,
`NOCI` тощо — це та сама схема, просто кожна група тепер — реальна
популяція нейронів FlyWire, а не один вручну підібраний вузол).

**Це ІНША модель симуляції, не просто "більше даних":** leaky
integrate-and-fire (LIF) — витік потенціалу (`leak=0.95`), поріг
(`threshold=1.0`), рефрактерний період (3 такти) — а не наш поточний
"накопичуй без витоку, поки не спрацює". Формат — CSR (compressed sparse
row) бінарник `connectome.bin.gz` (12.4МБ), Web Worker
(`js/sim-worker.js`, 486 рядків), і міст `js/brain-worker-bridge.js`
(652 рядки), який **уже написаний upstream-автором** саме для того, щоб
`BRAIN.stimulate.*`/`BRAIN.postSynaptic`/`BRAIN.drives` — той самий API,
яким написана НАША гра — працював без зміни ігрового коду. Це велика
перевага: не з нуля.

**КРИТИЧНИЙ РИЗИК, знайдений під час дослідження:** з 63 груп у РЕАЛЬНИХ
даних FlyWire **31 має 0 нейронів**. Серед них — саме ті, на яких
побудоване наше навчання цього тижня: `NOCI` (біль), `VIS_LC` (loom),
`MB_DAN_PUN` і `MB_MBON_AV` (punishment-навчання чашки/проводу!), а
також усі `DN_*`/`MN_LEG_*`/`MN_WING_*` (реальні мотонейрони ніг/крил
живуть у VNC — частині нервової системи, якої НЕМАЄ в публічному
датасеті FlyWire, лише мозок). Для рухів upstream-автор уже написав
компенсаційний шар (`synthesizeMotorOutputs()` у бридж-файлі — розподіляє
активність реальних центральних нейронів на віртуальні ноги/крила). Але
для punishment-навчання (чашка/провід) реального нейронного субстрату в
цих даних просто НЕМАЄ — це не питання часу, а питання наявності даних;
доведеться або спрощувати (напр. схема з ін'єкцією в реальні 5177
клітин Кеньйона `MB_KC` per-stimulus, як у snedea/flybrain), або
прийняти, що ці конкретні фічі лишаться на 70-групповій версії.

**Ненульові групи, які РЕАЛЬНО є (для орієнтиру):** `VIS_ME` (82318,
медула), `GENERIC_CENTRAL` (21955), `VIS_R1R6` (11487), `MB_KC` (5177),
`GNG_DESC` (3581, головний descending-вихід мозку), `MECH_BRISTLE`
(1927), `VIS_LPTC` (1907), `OLF_ORN_FOOD` (1851), `VIS_LO` (1793),
`CX_PFN`/`CX_FC`/`CX_HDELTA`/`CX_EPG` (central complex, локомоція:
1244/822/611/428), `OLF_PN` (699), `LH_APP` (559), `MB_DAN_REW` (335,
реальна нагорода — цей шлях ПРАЦЮЄ), `GUS_GRN_SWEET`/`GUS_GRN_WATER`
(214/131), `MB_MBON_APP` (96, апетитивне навчання — теж працює),
`GUS_GRN_BITTER` (65), `THERMO_COOL`/`THERMO_WARM` (54/49),
`DRIVE_HUNGER`/`DRIVE_FATIGUE` (46/34 — саме ці дві мають реальні
нейрони, `DRIVE_FEAR`/`DRIVE_CURIOSITY`/`DRIVE_GROOM` — ні, лишаються
чистими скалярами), `MN_PROBOSCIS` (24, реальний), `MN_ABDOMEN` (8).

**План по фазах (оцінка в годинах — наш реальний темп ітерації в цій
сесії, не "днів" людського проєкту; кожна фаза дешево перевіряється
headless-браузером тим самим методом, що й уся інша розробка):**

| Фаза | Що | Оцінка |
|---|---|---|
| 0. Спайк | Завантажити `connectome.bin.gz`+`sim-worker.js`+`brain-worker-bridge.js` у цей ворктрі, підняти локальний сервер, побачити live tick/fire state | 0.5–1 год — ✅ **ПІДТВЕРДЖЕНО 2026-09-16** |
| 1. База | Голод/зір/рух/їжа/політ через повний конектом, перекалібрувати `BEHAVIOR_THRESHOLDS`/`FEAR_SPIKE_*`/etc. під LIF-динаміку (пороги/константи з лумпованої моделі НЕ переносяться напряму), перевірити мобільно | 3–6 год |
| 2. Кастомне (чашка/провід/тарган/кава/навчання) | НЕ пряме перенесення — punishment-шлях (`MB_DAN_PUN`/`MB_MBON_AV`) без реальних нейронів, треба новий дизайн (напр. sparse-кодування в реальних `MB_KC`) | 4–10 год, дослідницьке, непередбачуване за визначенням |

### ✅ Фаза 0 підтверджена (2026-09-16)

Файли скопійовані з `github.com/snedea/flybrain` (`data/connectome.bin.gz`,
`data/neuron_meta.json`, `js/sim-worker.js`, `js/brain-worker-bridge.js`) у
цей ворктрі (папки `data/`, `js/`). Підключено до `fly-pixel-game.html`
одним новим `<script src="js/brain-worker-bridge.js"></script>` ПІСЛЯ
основного `<script>` блоку (BRAIN на той момент уже повністю визначений) +
тимчасовий inline-скрипт для спайк-верифікації (лічильник, що 15 разів
логує `BRAIN.workerReady`/`workerFiredNeurons`/`latestFireState` у
консоль — позначений коментарем `[FULL-CONNECTOME PHASE 0 SPIKE]`,
лишити чи прибрати — рішення Фази 1).

**Перевірено headless-браузером** (`python3 -m http.server 8791` + наш
Browser pane, НЕ через `preview_start`/launch.json — той падає з
`PermissionError: [Errno 1] Operation not permitted` на argparse
всередині пісочниці цієї сесії; прямий `python3 -m http.server` з Bash
працює нормально, це обхідний шлях для будь-якої майбутньої сесії):

- Консоль: `Connectome worker ready: 139255 neurons, 2698236 edges` —
  збігається з цифрами з дослідження вище, жодних помилок/warn.
- Мережа: `data/neuron_meta.json` і `data/connectome.bin.gz` — обидва 200 OK.
- **Живий стан підтверджено падінням значень у часі**, не статичний
  снепшот: `BRAIN.postSynaptic.DRIVE_HUNGER` 20→15, `GNG_DESC` (головний
  descending-вихід, реальні 3581 нейрони) 4.32→5.40,
  `BRAIN.workerFiredNeurons` 0→46 за 1.5с між двома замірами.
- Гра сама по собі малює муху на екрані монітора як зазвичай (скріншот
  перевірено) — тобто `runBrainTick`/ігровий цикл продовжує працювати з
  worker-based `BRAIN.update`, підміненим бриджем.

**Дрібниця, яку варто мати на увазі для Фази 1** (не блокер): миттєвий
`BRAIN.latestFireState` (Uint8Array по всіх 139255 нейронах з останнього
worker-тіку) в окремі секунди був повністю нульовим — worker тикає з
10Гц і зоною-гейтингом (`T7.7`), тож live-активність видно надійніше
через накопичені `postSynaptic`/`workerFiredNeurons`, а не миттєвий
`latestFireState` per-tick. `VIS_R1R6` (82к+11к нейронів) під постійною
стимуляцією (`lightLevel`) показував підозріло мікроскопічні значення
(~3.6e-8 частки) — можлива тема калібрування порогів/`WEIGHT_SCALE` у
Фазі 1, а не баг підключення.

### ✅ Фаза 1 підтверджена (2026-09-16) — голод/зір/рух/їжа/політ через повний конектом

Перевірено живою headless-симуляцією (тимчасовий `window.__brainLog`
ролінг-буфер у `runBrainTick`, гейтований `window.__brainLogOn` — вимкнений
за замовчуванням, нульова ціна в проді, лишений у коді як інструмент для
майбутніх сесій калібрування).

**Результат по кожному шляху (з реальними цифрами):**
- **Голод** — `BRAIN.drives.hunger` синхронізується з тілесним `hunger/100`
  щотакту, зростає 0.3→1.0 природно за ~70с ідла. Працює як і мало.
- **Їжа** — повний цикл підтверджено: смак/нюх (`foodNearby`) → підхід
  (`brainDir()`/steering) → `state='atfood'` на відстані < `FOOD_REACH_DIST`
  → `feedReady` (голод впав з 1.0 до **0.05** за один прийом їжі — саме
  той -0.3/такт `_isFeeding`-ефект). Сам ґейт `feedReady = brain.feed>8 OR
  (hunger>0.7 && foodNearby)` тепер спрацьовує майже завжди через ДРУГУ
  умову: `brain.feed` (`MN_PROBOSCIS`, реальні 24 нейрони) лишається 0, поки
  немає фізичного контакту з їжею (це очікувано — `GUS_GRN_SWEET`
  стимулюється лише в момент контакту, не на відстані нюху) — отже поріг
  `BEHAVIOR_THRESHOLDS.feed=8` фактично НЕ бере участі в рішенні їсти й не
  потребує зміни.
- **Втеча/політ** — після `Труснути стіл`: `DRIVE_FEAR` спайк (до 0.53),
  `brain.flight` (синтезований `synthesizeMotorOutputs()` з `GNG_DESC` +
  central-complex) підскочив до **140** (поріг `BEHAVIOR_THRESHOLDS.fly=15`
  — 9× з запасом), `mode` перемкнувся `ground→air`, і ПОВНИЙ цикл
  приземлення (calm-streak-based) підтверджено — `mode` повернувся на
  `ground` природно, без втручання. Значення 140 проти порогу 15 — великий
  запас, тобто втеча спрацьовує НАДІЙНО (не крихко на межі порогу); поріг
  можна було б підняти для більш градуйованої реакції, але це косметичне
  покращення, не потребує зміни для коректності.
- **Чистка (groom)** — природно спрацювала сама (без ручного тригера) під
  час 70с ідла, коли пил накопичився: `brain.groomAccum` максимум **8.17**
  проти порогу `BEHAVIOR_THRESHOLDS.groom=8` — щойно вистачає. Разом з
  ходьбою (`walkL/R` макс ~6.9, `state:'groom'` реально траплявся) —
  підтверджено живою поведінкою, не лише числом.

**Висновок по `BEHAVIOR_THRESHOLDS`:** усі три пороги (`feed:8`, `groom:8`,
`fly:15`) з ЛЕГАСІ 70-групової моделі ЛИШИЛИСЬ БЕЗ ЗМІН — жоден не
виявився фізично недосяжним чи зламаним під LIF-динамікою повного
конектома. `feed` і `fly` фактично не є вузьким місцем (перекриті іншими
умовами/великим запасом), `groom` влучив точно біля межі, як і мав.
Отже "перекалібрувати" в сенсі "поміняти числа" не знадобилось — сама
перевірка (чи ці числа взагалі досяжні під новою шкалою) і БУЛА
калібруванням, і вона пройшла.

**Знайдена й виправлена регресія (не калібрування порогів, а втрачена
фіча):** `BRAIN.firedThisTick` (масив "які нейрони спрацювали цього
такту", яким живиться рядок статусу під зонами) наповнюється лише
ЛЕГАСІ `BRAIN.runconnectome()` — а `workerUpdate()` з
`js/brain-worker-bridge.js` (upstream-файл, лишений verbatim) його
ніколи не чіпає. Без фіксу рядок статусу назавжди показував би "немає
активних нейронів" — регресія відносно вже реалізованої 70-групової
фічі. Виправлено В НАШОМУ файлі (не в upstream-бриджі, щоб той
лишався 1:1 копійованим): у `runBrainTick()` одразу після
`BRAIN.update()`, коли `BRAIN.workerReady`, самостійно перебудовуємо
`BRAIN.firedThisTick` з `BRAIN.postSynaptic` (немоторні групи з
значенням > 0 цього такту — worker-еквівалент легасі
`> BRAIN.fireThreshold`). Перевірено скріншотом: рядок реально показує
`DRIVE_CURIOSITY, CX_EPG, GNG_DESC, CX_PFN` замість заглушки. Побічний
ефект нульовий: `applyMBPlasticity()` шукає в цьому масиві
`MB_KC_HOT`/`MB_KC_WIRE`/`MB_DAN_PUN` — усі три мають 0 реальних
нейронів у конектомі, тож і далі ніколи там не з'являться (навчання
лишається коректно неактивним, як і задокументовано для Фази 2).

**Архітектурна знахідка, НЕ виправлена (потребує рішення користувача,
не проста зміна константи):** `brainDir()` (напрямок ходьби/польоту)
читає `bp('CX_HDELTA')/30` як "силу повороту". Виміряно: `CX_HDELTA`
стабільно **0** протягом 8+с спостереження за звичайних умов (без
вітру/різкого руху світла), так само як `VIS_LPTC=0`; `CX_PFN`/`CX_EPG`
— шумова підлога (~0.008). Причина НЕ в неправильному масштабі порога
(`/30`) — сам сигнал architecturally відсутній: у ЛЕГАСІ моделі
`CX_HDELTA` живився і від `DRIVE_CURIOSITY` (завжди ненульова, повільний
дрейф), а в повному конектомі `DRIVE_CURIOSITY` — віртуальна група БЕЗ
жодного реального нейрона (див. "критичний ризик" вище), тож фізично
не може синаптично збуджувати `CX_HDELTA` через справжні зв'язки
конектома — лише пряме читання `BRAIN.drives.curiosity` в
`workerUpdate()` для ЦІЄЇ Ж групи, не для низхідних. Практичний наслідок:
`turnMagnitude` майже завжди ≈0, тому напрямок повороту (`brainDir()`)
зараз практично повністю визначається лише випадковим `turnSign`/
`spread`-шумом — гра НЕ ламається (це той самий "чесний підкид монети"
фолбек, що вже був у коді для НАПРЯМКУ), але "мозок керує СИЛОЮ
повороту" зараз фактично не працює. Виправлення означало б написати
власний компенсаційний шар (аналогічний upstream-авторському
`synthesizeMotorOutputs()` для VNC) — це вже дизайнерське рішення
масштабу Фази 2, не одна зміна константи, тому свідомо НЕ зроблено
зараз без окремого підтвердження.

**Мобільна перевірка:** `resize_window` на 375×812, повне
перезавантаження — рендер коректний (муха на столі, кнопки, "Голод: %"),
рядок статусу показує реальні нейрони (див. фікс вище), жодних помилок
у консолі.

**Консоль:** 0 помилок/warn за всю сесію тестування (idle 70с + drop
food + eat cycle + shake table + flight + landing + mobile reload).

### ✅ Фаза 2 (частково) — CX_HDELTA-компенсація й punishment-навчання (2026-09-16)

За вибором користувача: (1) CX_HDELTA-компенсація — зробити зараз, разом
із Фазою 2; (2) punishment-навчання чашки/проводу — спрощена схема через
РЕАЛЬНІ клітини Кеньйона (`MB_KC`, 5177 нейронів), як в оригінальному
snedea/flybrain.

**CX_HDELTA (сила повороту).** У `runBrainTick()` (`fly-pixel-game.html`)
після `BRAIN.update()`: якщо реальний `bp('CX_HDELTA')` слабший за proxy
з РЕАЛЬНИХ сусідніх central-complex груп (`(CX_PFN+CX_FC+CX_EPG)/3 +
VIS_LPTC*0.5`) — пишемо proxy. Перевірено живим прогоном: `turnMagnitude`
(`min(1,CX_HDELTA/30)`) тепер коливається 0.21–0.67 замість постійного 0
(той самий підхід, що `synthesizeMotorOutputs()` у бриджі вже застосовує
до `GNG_DESC`).

**Punishment-навчання (чашка/провід).** Стара схема (псевдо-нейрони
`MB_KC_HOT`/`MB_KC_WIRE`/`MB_DAN_PUN`/`MB_MBON_AV` у `weights`) виявилась
**повністю мертвою** під worker-мозком — не лише `applyMBPlasticity()`
(як здавалось у Фазі 1), а взагалі ВЕСЬ ланцюжок: worker ніколи не
викликає `runconnectome()`/не читає `weights`, тож накопичені через
`dendriteAccumulate` значення нікому "запалити". Замінено на:
- Два фіксовані неперетинні підмножини по 60 РЕАЛЬНИХ `MB_KC`-нейронів
  (`kcHotIndices`/`kcWireIndices`) — обчислені з `BRAIN.workerGroupIdArr`.
- Новий хук `BRAIN.stimulateIndices(indices, intensity)` у
  `js/brain-worker-bridge.js` (чітко позначений як НАША, не upstream,
  правка — one-shot `V[idx]+=intensity` для довільних індексів, той самий
  механізм, що NOCI вже використовує, тож не конфліктує з бриджевим
  per-group `setStimulusState`).
- CS-спрацювання читаємо з РЕАЛЬНИХ LIF-спайків
  (`BRAIN.latestFireState` по нашій підмножині), US-ґейт
  (`mugPunishedThisTick`/`wirePunishedThisTick`) — напряму з фізики гри
  (`senseMug`/`senseWire`), а не з мертвого `MB_DAN_PUN`.
- Вагу зберігаємо в ТИХ САМИХ `weights.MB_KC_*.MB_MBON_AV` (ті самі
  ceiling/rate/decay, що й легасі) — лише спосіб росту інший.
- Вихід (`MB_MBON_AV` теж 0 реальних нейронів) синтезуємо напряму в
  `BRAIN.drives.fear` — той самий канал, що вже надійно веде до втечі
  (Фаза 1), не через мертву worker-пропагацію.

**Дві реальні проблеми знайдені й виправлені емпірично під час
перевірки** (не здогад — обидві відтворені живим прогоном):
1. **Aliasing між 100мс worker-тіком і 500мс перевіркою мозку.**
   `BRAIN.latestFireState` — миттєвий знімок останнього тіку; перевірка
   раз на такт мозку систематично пропускала короткі спрацювання (0/44
   впритул до чашки за 24с, хоча зовнішній частіший опитувач ловив
   спрацювання регулярно). Виправлено "липким" акумулятором
   (`kcPollHandle`, опитує кожні 80мс, `applyMBPlasticityWorker()` читає-
   і-скидає раз на такт) — тепер надійно ловить.
2. **`case 'stimulate'` у sim-worker.js активує групу лише РАЗ** (на
   першому виклику, поки вона не активна) — повторні уколи, поки група й
   так активна, НЕ освіжають її `groupCooldown`. Дрібні уколи (0.15, потім
   0.5) щотакту (500мс = 5 внутрішніх тіків) НІКОЛИ не встигали дотягнутись
   до порогу=1.0 — воркер деактивував групу й `V.fill(0,...)` обнуляв
   накопичення раніше. Виправлено підняттям інтенсивності (`KC_STIM_INTENSITY=3`)
   так, щоб КОЖЕН окремий укол сам перетинав поріг при максимальній
   близькості — надійний CS, а не крихке накопичення.

**Побічно знайдений і виправлений СПРАВЖНІЙ крах (не пов'язаний з
Фазою 2 напряму, але зупиняв усю гру, включно з мозком):**
`takeoff()` (`fly-pixel-game.html`) ніколи не скидала `frameIndex`.
`flightFrames` має 5 кадрів, `groomFramesA` — 6; зліт зі стану `groom` із
`frameIndex>=5` лишав недійсний індекс для `flightFrames[]` → рендер
кидав `TypeError`, який зупиняв `requestAnimationFrame` НАЗАВЖДИ (не
кадр — увесь цикл, включно з brain-тіками/голодом/усім). Відтворено
навмисне (`frameIndex=5; state='groom'; takeoff()`) і виправлено —
`takeoff()` тепер скидає `frameIndex=0`, той самий патерн, що вже є при
вході в `walk`/`groom`/`idle`.

**Перевірено живим прогоном (headless, ручні тести через JS-консоль,
позиція мухи тимчасово "пришпилена" до чашки/дроту для ізольованої
перевірки навчального контуру, як і в Фазі 1 для їжі):**
- Чашка: вага 2→6.64 (стеля 8) за ~3.5с безперервної близькості, страх
  0→0.278, фактичний зліт-втеча (`mode: 'ground'→'air'`) — повний
  замкнутий цикл CS→навчання→страх→втеча.
- Провід: вага 0→1.74 (перший удар), спад до наступного контакту, потім
  →3.18 (другий удар) — класична крива "вдар, навчись, забудь трохи,
  вдар знов", страх і втеча так само коректні щоразу.
- 0 помилок консолі за весь цикл тестів (окрім одноразового вже
  виправленого `frameIndex`-краху вище).

### ✅ Фаза 2 завершена — тарган і кофеїн (2026-09-16)

**Тарган (`kcRoachIndices`).** Той самий патерн, що mug/wire: ТРЕТЯ
неперетинна підмножина 60 реальних `MB_KC`-нейронів (індекси 120:180 з
тієї ж вибірки), стимулюється в `senseRoach()` — слабко в зоні
`ROACH_CLOSE_DIST` (дебаунс той самий, що й був для легасі `VIS_LC`),
максимально при фізичному контакті (`ROACH_TOUCH_DIST`, той самий момент,
що вже стимулює `DN_STARTLE`/`touch`). Вага — власна змінна
`kcRoachAvWeight` (не `weights.MB_KC_ROACH` — того запису НЕМАЄ навіть у
легасі `weights`, роадмап відкладав фічу ще до того, як там щось
з'явилось). **Головна причина, через яку відкладали цю фічу в 70-груповій
моделі — розділення джерел `VIS_LC` (стіна/тарган/клік ділили один
нейрон, навчений страх узагальнився б на звичайні стіни) — тут ФІЗИЧНО НЕ
ІСНУЄ**: кожен подразник має свою власну, неперетинну підмножину реальних
KC-нейронів за конструкцією, `VIS_LC` (застосовується й далі лише в
легасі-шляху для main/fallback) тут ні до чого не причетний.

Перевірено живим прогоном (пришпилена позиція до таргана): вага 0→1.72
(перший контакт), спад, →3.18 (другий контакт) — та сама крива, що й
wire, страх і втеча коректні щоразу, 0 крашів.

**Кофеїн під worker-мозком.** `CAFFEINE_PLASTICITY_MULT` (1.5) множить
`rate` у `applyMBPlasticityWorker()` так само, як у легасі — формула не
змінювалась при переписі на worker. Перевірено ІЗОЛЬОВАНО (форсовані
eligibility+punishment, один виклик функції, щоб виключити шум від
природного часу спрацювання CS): приріст без кофеїну **0.58**
(=0.6 rate − 0.02 decay), з кофеїном **0.88** (=0.6×1.5 − 0.02) — точний
коефіцієнт **1.5×**, як і задумано.

**Фаза 2 повністю закрита:** мозок мухи (голод/зір/рух/їжа/політ,
Фаза 1) + усе кастомне навчання (чашка/провід/тарган/кофеїн, Фаза 2) тепер
працюють через повний ~139k-нейронний FlyWire-конектом.

### ✅ 3D-візуалізація живої активації конектома (2026-09-16)

Нова фіча поза фазовим планом (окремий запит користувача) — жива 3D карта
активації повного конектома, з РЕАЛЬНИМИ просторовими координатами
нейронів FlyWire (не вигаданий/синтетичний layout).

**Джерело координат.** У тому самому репозиторії `snedea/flybrain`
лежить `data/coordinates.csv.gz` (`root_id, position [x y z], supervoxel_id`)
— публічні FlyWire-координати (voxel-простір FAFB), яких
`scripts/build_connectome.py` **взагалі не використовує** для побудови
`connectome.bin.gz`/`neuron_meta.json`. Перевірено: **100% збіг**
(139255/139255) по `root_id` з `data/neurons.csv.gz` (тим самим файлом,
чий порядок рядків визначає "оригінальний" індекс нейрона в
`connectome.bin.gz`). Підтверджено анатомічно: після застосування
group-sort перестановки нейрони `MB_KC` (тіла грибоподібних тіл)
утворюють щільний просторовий кластер (y: -38..-15, розмах ~23 одиниці)
проти випадкової вибірки того самого розміру з усього мозку (розмах
~197 одиниць) — координати реальні, не сміття.

**Побудова файлу (offline, одноразово, Python):**
`data/neuron_positions.bin.gz` — `neuron_count × 3 × float32` (x,y,z),
УЖЕ в тому самому group-sorted порядку, що worker використовує
внутрішньо для `BRAIN.latestFireState`/`workerRegionType`/`workerGroupIdArr`
(відтворено ТОЧНО той самий стабільний counting-sort за `group_id`, що й
`sim-worker.js` робить у `buildGroupStructures()` — розпарсено бінарний
формат `connectome.bin.gz` напряму: header, edges, потім per-neuron
`(uint8 region_type, uint16 group_id)`). Це означає: індекс `s` у файлі
позицій напряму збігається з індексом `s` у `BRAIN.latestFireState` —
жодного рантайм-ремапу не потрібно. Координати відцентровано й
масштабовано так, щоб найдовша вісь ≈200 одиниць (зручно для Three.js-
сцени). Розмір: 1.67МБ нестиснутий, **1.39МБ у gzip**.

**Рендер:** Three.js (CDN, `type="importmap"` для резолву внутрішнього
`import 'three'` в `OrbitControls.js`), `THREE.Points`/`BufferGeometry` з
усіма 139255 точками одним draw call. Базовий колір точки — за
`region_type` (sensory=бірюзовий, central=бурштиновий, drives=фіолетовий,
motor=оранжевий); при реальному спайку (з `BRAIN.latestFireState`,
опитується кожні 100мс — темп worker-тіку) колір миттєво йде до білого й
згасає геометрично (`×0.82` за оновлення, "фосфор"-ефект, щоб коротку
спайкову подію взагалі було видно оком). `OrbitControls` — тягни для
обертання, колесо — зум. Лінива ініціалізація: Three.js/файл позицій НЕ
завантажуються, поки користувач не натисне нову кнопку "3D мозок".

**Знайдена й виправлена проблема під час першого живого рендеру:**
початково використовував `THREE.AdditiveBlending` (типовий вибір для
"точок, що світяться") — але `VIS_ME` (медула) сама по собі складає
82318 із 139255 нейронів (59% усього мозку!), і в найгустіших ділянках
Additive складав кольори точок докупи до суцільного білого, ховаючи
кодування кольором за регіоном. Замінено на звичайне (не Additive)
змішування (`depthWrite:false`, щоб прозорі точки не "виїдали" одна одну
по глибині) — тепер обидва зорові частки (бірюзові) й центральний мозок
(бурштиновий) чітко різняться кольором, підтверджено скріншотом.

**Перевірено живим прогоном:** десктоп і мобільний (375×812) рендер
коректний, анатомічно впізнавана форма (два симетричні зорові частки +
центральна маса), `OrbitControls`-обертання працює (перевірено drag),
кількість активних нейронів у статус-рядку реально змінюється щотік
(0 → 2494 → 1851 → 0 → 46 → 0 за живий прогін), 0 помилок у чистій
(нова вкладка, без старої історії консолі) перевірці.

**Файли:**
- `data/neuron_positions.bin.gz` — нові дані (наш білд, не upstream)
- `fly-pixel-game.html` — новий блок `<script type="module">` в кінці
  файлу + `<button id="brain3dToggle">`/`#brain3dPanel` у розмітці,
  усе позначено `[FULL-CONNECTOME]`, окремо від upstream-коду
- Скрипт побудови `neuron_positions.bin.gz` НЕ закомічений у репозиторій
  (offline-білд у scratchpad цієї сесії, потребує `numpy` — встановлено
  локально через pip, не частина проєкту) — якщо файл колись треба
  перебудувати (напр. after оновлення `connectome.bin.gz`), логіка
  задокументована тут: розпарсити `connectome.bin.gz` для `group_id` в
  оригінальному порядку → стабільний counting-sort за `group_id`
  (реплікує `buildGroupStructures()`) → приєднати `coordinates.csv.gz` по
  `root_id` через `neurons.csv.gz` (порядок рядків = оригінальний індекс)
  → **PCA-переорієнтація** (власні вектори коваріаційної матриці хмари
  точок, відсортовані за спаданням дисперсії → X=найширша вісь,
  Z=найвужча; БЕЗ цього кроку — сирі voxel-осі FAFB дають видиму
  асиметрію лівої/правої частки через нахил у глибину, знайдено й
  виправлено за зауваженням користувача, див. нижче) → масштабувати за
  найширшою віссю до ~200 одиниць → застосувати group-sort перестановку
  → записати `float32×3` у group-sorted порядку.

### Виправлено після зауважень користувача (2026-09-16)

**1. "Чому виступ зліва більший за справа?"** Спершу невірно припустив, що
це артефакт повороту камери користувачем (OrbitControls) — користувач
слушно заперечив. Перевірив ПРАВИЛЬНО: підрахунок нейронів зліва/справа
(поріг x) показав ліва частка мала МЕНШЕ нейронів (54943 проти 63527
справа) — тобто це не реальна різниця в кількості. Далі перевірив глибину
(Z, вісь "до камери"): середній Z зліва = **12.3**, справа = **2.4** —
ліва частка ФІЗИЧНО ближче до камери в сирій системі координат FlyWire.
Причина: `voxel`-осі FAFB не вирівняні анатомічно (мозок трохи
"розвернутий" у глибину відносно довільно обраних x/y/z), а мій
початковий білд лише центрував і масштабував, без жодного повороту.

**Виправлено:** PCA-переорієнтація хмари точок (`numpy`, встановлено
локально для offline-білду) — знайдено справжні головні осі мозку
(власні вектори коваріаційної матриці), найширшу (bilateral, зорові
частки) вирівняно вздовж X, найвужчу — вздовж Z (глибина від камери).
Перевірено: різниця середнього Z між лівою й правою частками впала з
**~10 одиниць до ~0.9** — нахил фактично усунуто. Анатомічна перевірка
(кластеризація `MB_KC`) і надалі проходить після переорієнтації.
Перебудовано `data/neuron_positions.bin.gz`.

**2. "Синій/жовтий кольори — погано видно білу анімацію активації, і що
це за зони взагалі?"** Два окремі фікси:
- Базові кольори регіонів були занадто яскравими (низький контраст проти
  білого спалаху) — затемнено втричі-вчетверо (`REGION_BASE_COLOR`).
  Перевірено живим прогоном (Труснути стіл → 2494 активних нейрони):
  білий спалах тепер чітко виділяється на тлі темнішої хмари.
- Додано текстовий легенд-рядок під канвасом (`#brain3dLegend`):
  пояснює sensory (бірюзовий: зір/нюх/дотик/тепло) й central (бурштиновий:
  навчання/пам'ять/навігація) кольори та білий спалах = реальний спайк.
  (`drives`/`motor` кольори лишені в коді для повноти, але візуально
  майже не з'являються — у повному конектомі там майже немає реальних
  нейронів, див. "критичний ризик" вище.)

**Наступний крок:** немає активного плану — фаза "запиту користувача"
закрита. Можливі майбутні покращення (не заплановані, не почато): підпис
імені нейрона при наведенні/кліку на точку, фільтр за групою/регіоном,
таймлайн/запис активності замість лише live-режиму, кнопка "скинути
вигляд камери".

### ⚠️⚠️ КРИТИЧНА ЗНАХІДКА й ВИПРАВЛЕННЯ: глобальна нормалізація ваг у sim-worker.js крушила майже весь конектом (2026-09-16)

За зауваженням користувача ("виглядає наче дуже мало нейронів
активуються") провів повний аудит + виміряв причину до кінця.

**Аудит стимуляції.** З 32 груп із реальними нейронами МИ напряму
стимулюємо: дотик (`MECH_BRISTLE`), смак (`GUS_GRN_*`), нюх 1-го рівня
(`OLF_ORN_FOOD`/`DANGER`), навчання (`MB_KC` через kcHot/Wire/Roach),
зір 1-го рівня (`VIS_R1R6`). НІКОЛИ напряму: нюх 2-го рівня (`OLF_PN`
699, `OLF_LN` 453), зір глибших рівнів (`VIS_ME` 82318!, `VIS_LO` 1793,
`VIS_LPTC` 1907) — покладались винятково на реальну синаптичну передачу.

**Справжня причина, більша за прогалини стимуляції.** `sim-worker.js`
(upstream) нормалізував КОЖНУ вагу зв'язку відносно ЄДИНОГО глобального
максимуму (`maxAbsW`) серед усіх 2 698 236 ребер. Порахував: цей максимум
= **2405** — один-єдиний викид, тоді як медіана ваги = **8**, 99-й
перцентиль = **77**. Тобто типовий зв'язок ефективно ділився в ~300 разів
сильніше, ніж треба. Порахував теоретичний найкращий випадок (усі
вхідні партнери спрацювали одночасно, без leak): лише **11 з 82318**
нейронів `VIS_ME` могли б колись перетнути поріг=1.0. Перевірено живо:
`VIS_ME` показав **рівно 0** спрацювань за 3с безперервного сильного
світла (`VIS_R1R6` активно палав), попри 18136 реальних синапсів
`VIS_R1R6→VIS_ME` з непоганими вагами (5-15). Це ж пояснює й чому
`CX_HDELTA` виявився мертвим ще у Фазі 1 — та сама причина душить
практично весь "глибокий"/асоціативний конектом, не лише зір.

**Виправлено** в `js/sim-worker.js` (чітко позначено як відхилення від
upstream, з детальним поясненням прямо в коді): нормалізація тепер за
**99-м перцентилем** ваги замість єдиного максимуму (спершу пробував p99
— дало лише ~4.4% теоретичної досяжності `VIS_ME`, замало; порахував
p90 — дає ~30%, обраний компроміс), зі стелею `WEIGHT_SCALE*3`, щоб
рідкісні екстремальні ваги не давали патологічний одноразовий укол.
**Перевірено:** `VIS_ME` тепер реально спалахує 40-140 нейронів/тік під
світлом (розганяється за ~2с від 0), загальна "% firing" лишається
стабільною (1-2% на ідлі, не runaway-вибух).

**Побічний ефект, знайдений і виправлений під час перевірки:** підвищена
чутливість синапсів зробила `MB_KC` (5177 реальних нейронів) загалом
значно легше збудливим — фоновий трафік (їжа/зір/центральний комплекс)
почав випадково зачіпати наші довільно обрані 60-нейронні підмножини
(kcHot/Wire/Roach), навіть коли муха була деінде. Спершу спробував
поріг "≥20 із 60 підмножини спрацювали" — недостатньо, фон і далі
регулярно перетинав його (`kcHotTrace` лишався на максимумі щотакту,
`DRIVE_FEAR` повільно, але невпинно повз до 1.0, навіть у польоті далеко
від чашки). **Остаточне рішення:** eligibility trace й реакція страху
тепер гейтуються НАШИМ власним однозначним ігровим сигналом "CS зараз
присутній" (`mugCsPresentThisTick`/`wireCsPresentThisTick`/
`roachCsPresentThisTick`, виставляються в `senseMug`/`senseWire`/
`senseRoach` — та сама умова, що вже вирішує, чи стимулювати нейрони), а
не спробою відчитати це назад із зашумленої реальної активності мережі.
`BRAIN.stimulateIndices()` і реальні LIF-спайки нікуди не поділись —
просто не єдине джерело правди для "чи спрацював CS".

**Друга дрібна знахідка того ж сеансу:** `mugCsPresentThisTick`/
`wireCsPresentThisTick` (на відміну від `roachCsPresentThisTick`, який
сам скидається на самому початку `senseRoach()`) не скидались, коли
муха злітає — `senseMug()`/`senseWire()` просто не викликаються, поки
`mode!=='ground'`, тож прапорці лишались "заклякли" на своєму
останньому значенні (напр. `true`, якщо зліт стався прямо через чашку) і
навчання néкоректно тривало в польоті. Виправлено: явне скидання обох
прапорців (і `mugPunishedThisTick`/`wirePunishedThisTick`) у гілці
`else` (`mode!=='ground'`) у `runBrainTick()`.

**Перевірено остаточно (headless, ручний драйв `loop(t)`):** ідле подалі
від усіх трьох об'єктів — страх лишається РІВНО 0 15+ секунд поспіль;
чашка (`takeoff()` тимчасово застубльовано, щоб ізолювати перевірку
навчання від втечі) — вага 2→6.9 чистими сходинками, страх 0→0.567,
`mugCS` коректно true/false; після відходу від чашки — вага ЗАМОРОЖУЄТЬСЯ
на 6.9 (більше не росте), страх падає до 0 і лишається там.

**⚠️ Методологічний урок, наступений на граблі ЖИВЦЕМ у цій самій
сесії:** під час ручного драйву `loop(t)` синтетичним годинником (метод з
розділу "Методологія тестування" на початку файлу) забув скинути
`lastBrainTick` до свіжого `t` на старті нового прогону — рівно та
пастка, що й задокументована вище. Наслідок: `runBrainTick()`
(а значить і `senseMug`/`senseWire`/усе навчання) НЕ ВИКОНУВАВСЯ ЖОДНОГО
РАЗУ протягом кількох тестів (`t - lastBrainTick` лишався від'ємним),
даючи цілком правдоподібні, але повністю фальшиві "нуль-результати"
(було витрачено багато ітерацій на дебаг уявного бага, поки не
виявилась справжня причина). **Завжди `lastBrainTick = t;` (і
`lastHungerTick`/`nextPoopAt` за потреби) ПЕРШИМ рядком нового ручного
прогону.**

**Ще один практичний нюанс, знайдений цього ж сеансу:** якщо Browser
pane прихований (`document.hidden===true`, напр. через перемикання
вкладок інструментом), справжній `requestAnimationFrame` реально
призупиняється браузером (не наша помилка) — `hunger`/усе інше
буквально заморожується. У такому стані `loop(t)`/тести через
`window.addEventListener`/реальний час НЕ підходять — рятує лише ручний
синтетичний драйв `loop(t)` (з коректно скинутим `lastBrainTick`, див.
вище).

### Виправлено після зауваження користувача: "де взагалі видно повний список нейронів?" (2026-09-16)

Слушне зауваження — `js/brain-worker-bridge.js` (upstream, verbatim) із
самого початку САМ пише повний статус (прогрес завантаження →
`"139,255 neurons / 2,698,236 connections — FlyWire FAFB v783"` →
live-статистику "% firing, % active groups, ms/tick") у
`document.getElementById('connectomeSubtitle')` і `('scaleIndicator')`
— **АЛЕ ці елементи ніколи не існували в `fly-pixel-game.html`**, тож
усі виклики мовчки повертали `null` (код бриджа й так це передбачає —
`if (subtitle) {...}` — тому й не було жодної помилки в консолі, і Фаза
0/1 "зелена" перевірка це не зловила: я перевіряв через `BRAIN.*`
напряму в JS, а не те, що РЕАЛЬНО видно на сторінці). Користувач глянув
на сам браузер — і побачив ту саму стару 70-групову розмітку без жодного
підтвердження, що повний конектом узагалі підключений.

**Виправлено** (обидва елементи додано в `fly-pixel-game.html`, під
кнопками, над рядком статусу):
- `#scaleIndicator` — постійний рядок "139,255 neurons / 2,698,236
  connections — FlyWire FAFB v783" (з'являється одразу як worker готовий)
- `#connectomeSubtitle` — прогрес завантаження, потім живі
  live-статистики щотакту ("0% firing, 0% active groups, 0.4ms/tick" —
  0% firing тут ОЧІКУВАНО в конкретну мить читання, бо це миттєвий
  снепшот дуже розрідженого спайкінгу, не помилка — той самий нюанс, що
  задокументований у Фазі 0 про `latestFireState`)

**Друга, менш очевидна помилка, знайдена одразу під час перевірки
фіксу:** я спершу додав `#scaleIndicator { display:none; }` у CSS
(типова практика — ховати порожній елемент до заповнення), а бридж-код
"показує" його через `scaleEl.style.display = ''` — але порожній
INLINE-рядок лише ПРИБИРАЄ inline-override, а не форсує видимість:
елемент і далі лишався прихованим через CSS-правило (перевірено
`getComputedStyle().display === 'none'` і `getBoundingClientRect()`
нульового розміру, попри вже заповнений `textContent`). Прибрав
`display:none` з CSS — тепер `getComputedStyle().display === 'block'`,
реальні розміри на сторінці, підтверджено скріншотом.

**Урок для будь-якої майбутньої перевірки цього ворктрі:** підключення
через `BRAIN.*`/консоль/network — перевіряє, що ДАНІ живі, але НЕ
перевіряє, що це видно САМОМУ користувачу на сторінці. Обидві речі
потрібно звіряти окремо.

**Токени/ліміти (перевір `get_usage` на початку нової сесії, щоб знати
стартову точку):** ця робота токеномісткіша за одиницю "реальної
роботи", ніж 70-группова версія — читання незнайомого коду
(worker-протокол, бінарний парсинг), багато ітеративних тестів
незнайомої динаміки. Плануй окрему сесію під це, не тягни в одну з
основною розробкою.

**Джерело даних, якщо знадобиться заново:** `github.com/snedea/flybrain`
(MIT), релевантні файли — `data/neuron_meta.json`,
`data/connectome.bin.gz`, `js/sim-worker.js`, `js/brain-worker-bridge.js`.
Жоден із них ще не скопійований у цей ворктрі — це перший крок Фази 0.

## Технічні деталі конектома (базова 70-группова модель — контекст, не мета цієї гілки)

Мозок — дослівний перенос `js/constants.js` (вагова таблиця) і "легасі"
шляху `js/connectome.js` (`dendriteAccumulate → fireNeuron → runconnectome`)
проєкту [`snedea/flybrain`](https://github.com/snedea/flybrain) (MIT), той
самий такт 500мс (`BRAIN_TICK_MS`/`runBrainTick`), що й в оригіналі.

Тут, у цій гілці, саме ЦЕ й замінюється на повний конектом — деталі й
план вище, у розділі "🧪 Full-connectome experiment".

## Калібрувальні дані спрайтів (важливо для будь-яких змін анімації)

Кожен спрайт-аркуш має свою специфіку нарізки — **не всі кадри рівномірні
за шириною**. Дивіться коментарі біля `walkSheet`, `idleSheet`,
`groomFramesA`, `walkDiagFrames`, `walkDiag2Frames`, `flightFrames` у коді:
для кожного там або спільний масштаб на весь аркуш (walk/idle — рівномірні
кадри), або індивідуальний масштаб+анкор на кожен кадр (groom/діагоналі —
дуже нерівномірні через рух лапок/крил). Ці межі виведені аналізом
альфа-каналу вихідних PNG (шукали проміжки між фігурами), а не рівним
поділом — рівний поділ кілька разів спричиняв баги "протікання" кадрів.

Для flight-кадрів є додатковий нюанс: НЕ рахуйте масштаб окремо на кожен
кадр за bodyH, коли крила міняють форму (не висоту) — це "роздуває" тіло
візуально. Використано єдиний спільний масштаб (`FLIGHT_REF_BODY_H`) для
всього аркуша, з індивідуальними лише центрами позиції.

Той самий per-frame-anchor підхід (`groomFrame()`) повторно використаний
пізніше для таргана й лазіння — деталі й виміряні координати кожного
конкретного аркуша шукай у `CHANGELOG.md`.

## Що НЕ реалізовано (свідомо відкладено)

- Реальне "поїдання" їжі (їжа не зникає після `atfood`, лише хунгер-статистика
  й нагорода з `MB_DAN_REW` оновлюються)
- Справжня фізика польоту (гравітація/втома — стилізовані, не SI-одиниці)
- planck.js фізика тіла з 6 ногами (була в оригінальному плані архітектури,
  ще не почата)
- ~~Повна 139k-нейронна модель flybrain через Web Worker~~ — **ЗРОБЛЕНО
  2026-09-16 у ЦІЙ гілці**, див. "🧪 Full-connectome experiment" вище
  (цей пункт стосувався тільки `main`/70-групової версії, де й далі
  свідомо не підключено — там self-contained/`file://` важливіші)
- Навчання таргана (`MB_KC_ROACH`) — за прямою вказівкою користувача,
  свідомо востаннє в черзі: лише ПІСЛЯ підтвердження, що патерн навчання
  на чашці/проводі справді працює довше, І після розділення джерел
  `VIS_LC` (наразі стіни/таргана/клік усі штовхають той самий спільний
  нейрон — без розділення навчений страх від таргана хибно узагальнився б
  на звичайні стіни). Див. пам'ять `fly-learning-roadmap`.
### ✅ Швидкість польоту, груминг-баг, смакові профілі, спрага (2026-09-17)

**Швидкість польоту +15%.** `FLIGHT_SPEED_MULT`: 2.904 → 3.3396. Витрату
витривалості не чіпав (та сама політика, що й за попередній +20%).

**Груминг-баг (та сама категорія, що й VIS_ME/CX_HDELTA раніше).**
Користувач повідомив: чиститься майже одразу, хоча тригер мав бути на
`DUST_GROOM_TRIGGER=45%` пилу. Перевірив живо: `accumGroom` перетинав
старий поріг (`BEHAVIOR_THRESHOLDS.groom=8`) вже на dust≈15% — той самий
побічний ефект p90-нормалізації ваг (`synthesizeMotorOutputs()`/
`groomIntent` стали загалом чутливішими до `GNG_DESC`). Підняв поріг до
**22** (лінійна екстраполяція виміряного співвідношення groomAccum/dust).
**Важлива знахідка при перевірці:** сигнал НЕ ідеально лінійний — базовий
рівень (плато ~3.87, стабільний багато тактів) тепер надійно нижче
нового порогу (більше не чиститься "просто від часу"), але випадкові
сплески (та сама шумова природа, що ми вже бачили з kcHot/Wire/Roach)
іноді ще проскакують поріг РАНІШЕ за dust=45% — повної пропорційності
дуже точно досягти неможливо через властиву шумність LIF-сигналу після
підвищеної чутливості, але головна скарга ("чиститься постійно
незалежно від пилу") виправлена.

**⚠️ ПЕРЕВИПРАВЛЕНО (2026-09-17, за повторною скаргою "й досі чистить
лапки на кожен 1% від нуля"): фікс вище (8→22) лікував НЕ справжню
причину, а лише збіг.** Живе трасування (`Math.motorcontrol` обгорнутий
для читання `abdomen`/`legL1`/`legR1`/`head` до їх обнулення в
`readMotor`) знайшло дві реальні, окремі причини:
1. **`DRIVE_GROOM` у worker-мосту НІКОЛИ не був насправді підключений до
   `dust`.** `js/brain-worker-bridge.js`'s "virtual group bypass" (група
   без живих нейронів) пише `BRAIN.postSynaptic['DRIVE_GROOM'] =
   BRAIN.drives.groom * FIRE_STATE_SCALE` щотакту — а `BRAIN.drives.groom`
   росте АВТОНОМНО (`d.groom += 0.008` щотакту в `BRAIN.updateDrives()`,
   оригінальний flybrain-код, ще з часів ДО появи `dust` у цій грі) —
   ПОВНІСТЮ незалежно від `dust`. dust-гейтований виклик
   (`BRAIN.dendriteAccumulateScaled('DRIVE_GROOM', ...)` в `runBrainTick`,
   активний лише коли `dust>45`) НЕ пише в сам `DRIVE_GROOM` — той метод
   лише розподіляє ВИХІДНІ ваги `DRIVE_GROOM` (в `SEZ_GROOM` і т.д.),
   тобто dust ніколи не був справжнім джерелом чищення — лапки чистились
   за власним годинником конектома. Фікс: `BRAIN.drives.groom = dust/100`
   в `runBrainTick` (fly-pixel-game.html) — той самий патерн синхронізації,
   що вже є для `hunger`.
2. **Навіть після (1) чищення все одно спрацьовувало рано (dust≈13%).**
   `accumGroom = abdomen + (abdomen>0?head:0) + Math.min(legL1,legR1)` —
   а `synthesizeMotorOutputs()` (`js/brain-worker-bridge.js`) розподіляв
   ЗВИЧАЙНИЙ сигнал ходьби (симетричний, лише ±4% jitter) на ВСІ 6 пар
   лапок, включно з L1/R1 — тож `min(legL1,legR1)` був майже завжди
   додатним ПРОСТО ВІД ХОДЬБИ, а не від реального чищення. В оригінальній
   70-груповій моделі це не було проблемою (там ходьба живила L1/R1 лише
   малою фіксованою вагою `CPG_MASTER: 4`, а не всім `walkDrive`). Фікс:
   walk-сигнал тепер іде тільки в L2/L3+R2/R3 (той самий сумарний
   `walkDrive`, поділений на 2 замість 3 ноги/бік — на
   `accumWalkLeft/Right`, отже й на швидкість ходьби, це НЕ впливає), L1/R1
   лишаються "чистим" груминг-індикатором.

Після ОБОХ фіксів живий тест (Math.random примусово зафіксований —
прибирає випадкові пилові пориви й jitter, лишає тільки реальний шум
конектома) показав: `groomAccum` і досі шумно коливається в діапазоні
~20-60 незалежно від dust у діапазоні 0-80% (легкий зсув вгору після
dust=45, коли вмикається `SEZ_GROOM`, але сигнал/шум замалий для чистого
розділення) — залишковий шум самої мережі: `MN_HEAD`/`MN_ABDOMEN`
отримують непов'язану з пилом активність десь із реального конектома
(та сама "все стало загалом чутливішим" природа, що й підозрювалась
раніше за p90-нормалізацію — просто помилково приписана "рішенню" (22),
яке насправді просто збіглося з шумом того ж порядку). Підняв поріг
**22 → 50** (вище типового шумового плато при dust<40%). Живий тест із
УВІМКНЕНОЮ рандомізацією (реальні умови гри, 2 цикли чищення): тригери на
dust≈58%, 39%, 49% — і один рідкісний edge-case одразу після попереднього
чищення (dust≈1%, залишковий сплеск шуму + великий випадковий поривий
пилу одразу після скидання). Величезне покращення проти "кожні 1%", хоча
й не ідеальна пропорційність — та сама властива LIF-шумність, що й
раніше, тепер куди точніше локалізована (не вигадана "чутливість
GNG_DESC", а конкретно MN_HEAD/MN_ABDOMEN noise floor) і зменшена
настільки, наскільки дозволяє архітектура двох фіксів вище.

**Смакові профілі їжі (за прямою вказівкою користувача, з поясненням
реальної біології):**
- **Яблуко** — солодке (цукор) + легка водяниста нотка (~85% води в
  реальному яблуці): `GUS_GRN_SWEET` (повна) + `GUS_GRN_WATER` (×0.3).
- **Кавова пляма** — ПЕРШ ЗА ВСЕ вода (джерело для спраги, не їжі) з
  кофеїном (гіркота) і трохи цукру: `GUS_GRN_WATER` (повна) +
  `GUS_GRN_BITTER` (×0.4, як і було) + `GUS_GRN_SWEET` (×0.25, НОВЕ).
  Ефект теж перевернуто: тепер ПЕРШ ЗА ВСЕ знімає СПРАГУ
  (`THIRST_RELIEF_COFFEE=70`), голод — як і раніше, малим
  `COFFEE_HUNGER_RELIEF=15`.
- **Салямі** — м'ясо, НЕ солодке (виправлено: раніше помилково ділило
  ОДИН `foodContact`→`GUS_GRN_SWEET` шлях із яблуком). Новий окремий
  прапорець `BRAIN.stimulate.meatContact` → лише `GUS_GRN_WATER` (×0.4,
  жир/волога в'яленого м'яса). **Дослідив датасет:** реального
  смакового рецептора "білок/умамі" в 63-груповій таксономії FlyWire
  НЕМАЄ (`GUS_GRN_*` — лише SWEET/BITTER/WATER, повний список).
  `GUS_GRN_BITTER` НЕ підходить (веде до `MB_DAN_PUN`/`DRIVE_FEAR` —
  карає, а м'ясо для мухи не огидне) — `GUS_GRN_WATER` чесний доступний
  компроміс: помірна, реальна, але менша (MB_DAN_REW:3 замість
  SWEET-івських 7) нагорода, чітко відмінна від солодкого.

**Знайдений і виправлений попутно справжній мертвий шлях:**
`BRAIN.stimulate.waterContact` (смак кавової плями) НІКОЛИ не був
підключений у worker-гілці бриджа (`collectStimulationSegments()`) — той
самий клас бага, що mug/wire/roach до Фази 2 (легасі
`dendriteAccumulate`-шлях працював, worker — ні). Виправлено в
`js/brain-worker-bridge.js` (чітко позначено як наше game-specific
доповнення, не з upstream).

**Спрага (за прямою вказівкою користувача: "муха в реальному світі
відчуває спрагу, і в нас є цей нейрон [`GUS_GRN_WATER`, 131 реальних
нейронів] — варто додати").** Новий бар "Спрага: %" (той самий стиль, що
голод/пил), проста безперервна фізіологія БЕЗ фаз (на відміну від
голоду — реальна спрага не має аналога "харчової коми"),
`THIRST_RATE=100/110` (досягає 100% за ~110с). **Важлива архітектурна
межа, задокументована прямо в коді:** немає окремої `DRIVE_THIRST`-групи
в 63-груповій таксономії FlyWire (на відміну від
HUNGER/FEAR/FATIGUE/CURIOSITY/GROOM, які там є, хай навіть віртуальні) —
це межа самого датасету, не наше архітектурне рішення, тож `brain.thirst`
не синхронізується через `BRAIN.drives.*`, як голод, а йде напряму
тіло→знімок. `findNearestFoodInRange()` розширено: спрагла-але-не-голодна
муха тепер шукає САМЕ каву (єдине джерело води), ігноруючи тверду їжу;
голодна-але-не-спрагла й далі ігнорує каву, як раніше. `feedReady`
розширено: спрага > `THIRST_DRINK_THRESHOLD` теж може вирішити "пити".

**Перевірено живим прогоном (ручний драйв `loop(t)`, з коректним
скиданням `lastBrainTick`/`lastHungerTick` — див. урок вище):**
- Салямі: `GUS_GRN_SWEET` лишається РІВНО 0 увесь час поїдання (голод
  90→0), `GUS_GRN_WATER` — малий, ненульовий сигнал.
- Кава (спрагла 80%, не дуже голодна 30%): спрага 82→12 (−70, точний
  `THIRST_RELIEF_COFFEE`), голод 33→18 (−15, точний
  `COFFEE_HUNGER_RELIEF`) — жодних від'ємних значень при чистому стані.
- (Перше вимірювання показало від'ємні спрага/голод — виявилось
  забрудненим станом від попередніх тестів у ЦІЙ ЖЕ довгій сесії
  (залишкові `setInterval`-опитувачі), не реальним багом — передрогон з
  чистого перезавантаження підтвердив коректну поведінку й
  `Math.max(0,...)`-затиск.)

---

### ✅ Сенсорні нейрони спалахують "все-або-нічого" замість плавної активності (2026-09-17)

Користувач подивився на 3D-мозок і запитав: чому "2% firing", і чому
навіть маючи купу сенсорних тригерів (зір/нюх/дотик/температура/біль),
на візуалізації переважно активний ЦЕНТР, а не периферія (хоч
сенсорні групи — 75% усього мозку, 104,257/139,255, VIS_ME сам один —
59%).

**Знайдено живим замірюванням по групах** (обгорнув `BRAIN.motorcontrol`,
рахував fired-нейронів по групі кожен такт): дві РІЗНІ причини, які
легко сплутати:
1. **Це нормально** — дотик/біль/таргана-в-полі-зору — це РІДКІСНІ,
   короткі події; коли вони не відбуваються прямо зараз, відповідні
   нейрони МАЮТЬ мовчати. Не чіпав.
2. **Це реальна проблема** — "постійні" відчуття (зір оточення, нюх,
   температура) мали б хоч трохи жевріти майже завжди, а натомість
   довго повністю мовчали, а РАЗ НА ЯКИЙСЬ ЧАС весь гурт спалахував
   практично одночасно. Пряме вимірювання це підтвердило: один
   зафіксований такт — **11,341 з 11,487 нейронів VIS_R1R6 (99%)**
   спрацювали ОДНОЧАСНО, тоді як майже всі інші такти — 0-2.

**Корінна причина:** у `sendStimulation()` (`js/brain-worker-bridge.js`)
КОЖЕН нейрон групи щотакт отримував АБСОЛЮТНО ІДЕНТИЧНУ інтенсивність
стимуляції (`allIntensities[k] = seg.intensity`, без жодної варіації) —
а в LIF-динаміці (`js/sim-worker.js`, `leakRate=0.95`, `threshold=1.0`)
NEMAЄ жодного власного шуму на рівні нейрона. Тож коли інтенсивність
слабка (масштабована на `lightLevel` тощо, типово 0.05-0.1), кожному
нейрону потрібно ~15-16 ПОСЛІДОВНИХ тактів безперервної стимуляції, щоб
дозарядитись до порогу — і оскільки всі 11,487 нейронів заряджаються за
ІДЕНТИЧНОЮ траєкторією, вони всі перетинають поріг НА ТОМУ Ж САМОМУ
такті. Реальні фоторецептори мають природний шум (тепловий/синаптичний)
— тому спрацьовують у трохи різний момент, і сигнал виходить плавним,
розмазаним у часі, а не бінарним "усе чи нічого".

**Фікс:** додав рандомізований джитер на інтенсивність кожного нейрона
при кожному виклику `sendStimulation()` — `intensity * (0.7 +
Math.random()*0.6)` (0.7×-1.3×, перегенерується щотакту мозку, ~500мс).
Середня інтенсивність по групі не змінюється, але різні нейрони тепер
дозаряджаються за РІЗНУ кількість тактів — популяція розтягується в
хвилю замість миттєвого спалаху.

**Перевірено живо** (стабільний `lightLevel=0.6`, `findLightPull`
тимчасово підмінено на фіксоване значення для чистоти виміру):
`VIS_R1R6` fired-по-тактах: `2,4,132,2821,1770,133,12,4,14,483,1493,
1985,820,222,21,32,284,852,1513,1264,477,165,64,183,521` — хвилеподібна
картина (пік ~25% групи, а не 99%), майже завжди хоч трохи ненульова,
замість "довга тиша → миттєвий спалах на всю групу". Рідкісні події
(дотик/біль/таргана) НЕ чіпав — той самий код-шлях, але для одноразових
пульсів синхронність не має значення (подія й так триває один такт).

**Продовження — фонове освітлення кімнати (та сама розмова, 2026-09-17).**
Джитер вирішив СИНХРОННІСТЬ, але не саму причину "1.47% в середньому":
`VIS_ME` (59% усього мозку) майже завжди мовчить, бо `BRAIN.stimulate.
lightLevel` був РІВНО 0 всюди поза двома вузькими радіусами (монітор/
вікно) — фоторецептори (`VIS_R1R6`) практично ніколи не бачили нічого,
крім цих двох плям. Заміряно: без VIS_ME решта мозку працює 3.29%, з
VIS_ME (домінує кількістю) — лише 1.47% загалом.

Користувач слушно зауважив: світло в кімнаті не лише пряме — воно
відбивається від стелі/стін/стола, тому реальна муха завжди має хоч
якийсь фоновий сигнал, не тільки "0 або пряме джерело". Додав
`AMBIENT_LIGHT = {day:0.25, night:0.05}` (біля `LIGHT_SOURCES`) —
`Math.max` (НЕ сума) з реальним джерелом у `runBrainTick`, тобто
джерело зблизька завжди домінує, фон лише не дає впасти до нуля
далеко від обох. День — вище порогу стимуляції сенсорів (0.2 в
`collectStimulationSegments`), ніч — свідомо нижче (кімната й справді
темна вночі без джерел). **Свідомо НЕ чіпав `findLightPull()`** (та
сама функція, що дає напрямок "тягне до світла" для фототаксису) —
фон впливає лише на `BRAIN.stimulate.lightLevel` (що бачить/відчуває
око), а не на поведінку "curiosity тягне ДО джерела" — інакше фон
змусив би муху тягнутись у порожнє місце.

Перевірено живо (муха в дальньому кутку, 400,500 — поза обома
радіусами): `lightLevel` тепер стабільно 0.25 замість 0, `VIS_ME`
показує неперервну, наростаючу активність (0→2→11→35→98→...→291)
замість повної тиші; `findLightPull(400,500)` і далі повертає `null`
(поведінка фототаксису не зачеплена).

Обговорювали й ідею користувача зробити оголений провід джерелом
світла/тепла (іскра ж) — свідомо ВІДХИЛЕНО як постійне джерело: провід
навмисно "чистий" CS без вродженої небезпеки (`MB_KC_WIRE →
MB_MBON_AV: 0`, Фаза 2 навчання страху) саме для того, щоб страх перед
ним був ПОВНІСТЮ навченим, а не вродженим — постійне тепло/світло
ризикувало б "узагальнити" навчений страх на звичайну електроніку
(та сама небезпека, що явно описана в сусідньому коментарі про
THERMO_MUG). Компроміс (короткий спалах світла САМЕ в момент удару
струмом, одночасно з NOCI) — обговорено, але ще НЕ реалізовано.

---

**Продовження — дешеві варіанти (2)+(3) з "візуальної складності" (та сама розмова, 2026-09-17).**
- **(2) Неперервний оптичний потік.** `VIS_LPTC` у worker-шляху
  (`js/brain-worker-bridge.js`) був пласким пульсом
  (`lightLevel>0.1 && _isMoving → 0.3`), ігноруючи вже готовий
  `BRAIN.stimulate.opticFlow` (справжній градієнт яскравості між поточною
  позицією і точкою попереду по курсу, `brightnessAt()`) — той самий клас
  мертвого шляху, що mug/wire/waterContact: легасі-шлях
  (`BRAIN.update()`) вже читав `opticFlow`, worker — ні. Підключив.
- **(3) Текстура поверхонь.** Додав `ZONE_REFLECTIVITY` в
  `brightnessAt()` — дрібна локальна поправка яскравості за типом
  поверхні (дерево столу/кахель підлоги/пластик боку ПК/кераміка
  чашки), НЕ зачіпає `findLightPull()`/фототаксис. Тепер оптичний потік
  має сигнал навіть далеко від джерел світла — просто від переходу між
  зонами під час руху.

**Перевірено живо:** `brightnessAt()` коректно повертає різні значення
за зоною (walk=0.03, floor=0.05...); `VIS_LPTC` дає стабільно ненульове
спрацювання (2-24/такт) під час руху далеко від обох джерел світла,
замість фіксованої константи.

**Чесний результат:** загальний "% firing" усього мозку НЕ зрушився
помітно (1.47%→1.48%) — і це очікувано, а не провал фіксу: обидва
дешеві варіанти покращують ЯКІСТЬ/градуйованість сигналу (менше
"мертвих" зон, менше пласких констант), а не сам агрегований відсоток,
який структурно обмежений розміром VIS_ME (59% мозку) і рефрактерним
періодом (стеля ~25%) — див. попередній розділ. Дорожчий варіант (1,
"віяло променів замість одного") лишається неспробуваним — саме він
може дати відчутний зсув у самому числі, якщо це стане наступним
кроком.

---

**Продовження — (1) "віяло променів" замість одного скаляра (та сама розмова, 2026-09-17).**

Дорожчий варіант із трьох. Знайшов, що `senseSurroundings()` (fly-pixel-
game.html) ВЖЕ кастить `VISION_RAY_COUNT=8` променів навколо мухи — але
одразу стискає їх в ОДНЕ число (`nearestEdgeDist`, мінімум по всіх
напрямках) до того, як воно доходить до мозку. І ще гірше: сам `VIS_LO`
(1,793 живих нейрони) виявився ЩЕ ОДНИМ повністю мертвим шляхом у
worker-гілці — `collectStimulationSegments()` (`js/brain-worker-bridge.js`)
взагалі не мав жодного рядка з `VIS_LO`/`VIS_LC` (легасі-шлях
(`BRAIN.update()`) це стимулював, worker — ні; той самий клас бага, що
mug/wire/waterContact/opticFlow раніше).

**Що зробив:**
- `senseSurroundings()` тепер повертає `rayDists` — усі 8 значень окремо
  (раніше — лише мінімум).
- `ensureVisLoRaySubsets()` — ділить 1,793 реальних нейрони `VIS_LO` на
  8 фіксованих підмножин (~224 кожна), той самий перевірений патерн, що
  й `kcHotIndices`/`kcWireIndices`/`kcRoachIndices` (`ensureKCSubsets`)
  — відтворюваний поділ, не випадковий щоразу.
- `stimulateVisionFan(rayDists)` — кожен промінь б'є СВОЮ підмножину,
  інтенсивність пропорційна близькості (`BRAIN.stimulateIndices`, той
  самий одноразовий канал прямої ін'єкції V, що й kcHot/kcWire), викликано
  з `runBrainTick` одразу після `senseSurroundings()`.

Тепер напрямок зору — це справжня ретинотопія (сусідні промені → сусідні
нейрони, як реальні сусідні омматидії проектуються в сусідні колонки
медули), а не один скаляр "найближчий край хоч десь".

**Перевірено живо** (муха біля краю канви, щільне опитування — один
знімок `BRAIN.latestFireState` не ловить одноразовий спайк, та сама
пастка aliasing, що й раніше в сесії): різні промені дають КІЛЬКІСНО
різну активацію, корельовану з відстанню — `avgDistPerRay: [116,71,83,
63,103,80,88,99] → avgFiredPerRay: [100,167,147,181,102,138,131,130]`
(найближчий промінь far=63 → 181 спрацювань з 224 у вікні; найдальший
far=116 → лише 100) — чітка обернена кореляція, не шум. `VIS_LO` за
одне вікно тепер залучає 45-80% своєї підмножини замість колишніх
0.4-0.7% базового рівня.

Загальний "% firing" (той, що показує статус-рядок, single-snapshot
метрика) зрушився скромно (1.47%→1.57-1.78%, шумно між прогонами) —
очікувано: та сама методологічна пастка "миттєвий знімок пропускає
одноразові спайки" применшує ефект тут так само, як і для VIS_R1R6
раніше; справжнє залучення VIS_LO (виміряне щільним опитуванням) значно
більше, ніж видно в одному кадрі.

---

### ✅ Яблуко й салямі тепер реально впливають на спрагу (2026-09-17)

За прямою вказівкою користувача, з детальним поясненням реальної
фізіології дрозофіли (гігрорецептори/густаторні осморецептори на
лапках і хоботку, нейрони спраги типу Leucokinin):
- **Яблуко** (~85% води — "їжа і питво два в одному", та сама причина,
  чому в лабораторних культурах дрозофіл кладуть шматочки яблука проти
  зневоднення) — тепер РЕАЛЬНО гасить спрагу, не лише голод:
  `THIRST_RELIEF_APPLE = 50`.
- **Салямі** (сіль — осмотичний стрес, витягує воду з гемолімфи;
  перетравлення білка/жиру вимагає ДОДАТКОВОЇ води для виведення
  азотистих відходів) — спрага тепер РОСТЕ після їжі, не просто
  лишається без змін: `THIRST_PENALTY_SALAMI = 22`.

Обидві константи визначені біля `THIRST_RELIEF_COFFEE`
(fly-pixel-game.html), застосовуються в тих самих `isSalami`/`else`
(яблуко) гілках блоку `state==='atfood'`, де вже застосовувались
голод/дофамін/смакові прапорці.

**Перевірено живо:** яблуко — спрага 60%→10.5% (−50, точно
`THIRST_RELIEF_APPLE`); салямі — спрага 40%→62.5% (+22, точно
`THIRST_PENALTY_SALAMI`). Емерджентний ефект: тепер після салямі муха
природно захоче саме кави (єдиного джерела води) значно раніше, ніж
раніше.

---

### ✅ Реальний сон (повний цикл із боргом) і легке передозування кавою (2026-09-17)

За прямою вказівкою користувача, з двома детальними поясненнями реальної
сомнології/фізіології дрозофіли. Перед реалізацією уточнив через
AskUserQuestion обсяг (обрано: повний цикл сну з боргом; легкий ефект
передозування, без смерті).

**Сон.** Немає реального нейрона "октопамін" (чи будь-якого
сон-специфічного) у 63-груповій таксономії FlyWire — та сама межа
датасету, що й раніше з DRIVE_THIRST; чесно використав ВЖЕ ІСНУЮЧІ
`BRAIN.drives.fatigue` (інверсія — потреба спати) і `dopamine`
(збудження/пробудження) як практичний, не вигаданий, замінник.
- Новий стан `'sleep'` — входить вночі (`isNight`, замінив мертвий
  `lightLevel===0`-проксі, зламаний після AMBIENT_LIGHT) при НИЖЧОМУ
  порозі втоми (`SLEEP_FATIGUE_NIGHT=0.35`), ніж звичайний денний ідле
  (`restFatigue=0.7`), АБО коротка сієста одразу після ситної їжі
  (`satietyPhase==='coma'`, `SIESTA_FATIGUE_DAY=0.55`).
- **Борг сну** (`sleepDebt`, 0-100): росте, коли вночі не спить
  (`SLEEP_DEBT_GROWTH_PER_SEC`), гаситься ШВИДШЕ, поки РЕАЛЬНО спить
  (`SLEEP_DEBT_RECOVERY_PER_SEC` — компенсаторний сон ефективніший, як і
  в людей після недосипу). Тривалість сну росте і з поточною втомою, і з
  боргом (`SLEEP_DEBT_BONUS_MS`) — rebound.
- **Складніше розбудити** — `SLEEP_AROUSAL_DAMPING=0.3` применшує ВХІДНИЙ
  стимул (клік/струс) під час сну, не сам нейронний розрахунок (реальний,
  просто слабший вхід — сенсорний гейтинг). Підібрано так, щоб клік
  (0.15×0.3=0.045) і слабкий струс (0.25×0.3=0.075) НЕ будили, а сильний
  струс (0.4×0.3=0.12) — будив (поріг `SLEEP_WAKE_FEAR_THRESHOLD=0.1`).
  **Знахідка при перевірці:** початкова версія читала `BRAIN.accumFlight`
  одразу після `BRAIN.update()` — той самий відомий лаг
  однократних-BRAIN.update()-викликів (worker-пайплайн не встигає
  відобразити щойно виставлений `BRAIN.drives.fear` в ОДНОМУ такті),
  тож НАВІТЬ сильний струс не будив. Замінив на прямий scalar-поріг
  `brain.fear` (виставляється прямо в коді, без затримки) — перевірено
  живо: слабкий струс → лишається спати (fear=0.075), сильний → злітає
  (fear=0.12, mode='air').
- Сон відновлює втому ефективніше за ідле (`SLEEP_FATIGUE_RECOVERY_MULT
  =1.8`, `BRAIN._isSleeping` в `BRAIN.updateDrives()`).
- Хронічний борг сну послаблює навчання (реальна дрозофіла: недосип
  "порушує пам'ять") — `sleepDeprivationMult()` масштабує КОЖЕН
  CS-сигнал для MB-пластичності mug/wire/roach (`KC_STIM_INTENSITY()`,
  було const, тепер функція; підлога 0.3× на повному боргу).
  Скорочення життя з дослідження користувача НЕ моделюємо — у грі
  взагалі немає механіки віку/смерті від старості.
- Візуально — та сама анімація idle (нової пози немає, за домовленістю з
  користувачем), лише повільніше гортання кадрів (700 vs 350мс).

**Перевірено живо:** нічний сон при fatigue=0.6 → входить одразу
(тривалість ~7.6с, збігається з формулою); fatigue плавно 0.6→0 за час
сну (прискорене відновлення); `sleepDebt` НІКОЛИ не йшла в мінус
(clamp коректний); після природного пробудження вночі борг знову росте.

**Кава — легке передозування.** П'є каву, поки `caffeineUntil` ще не
сплив (кофеїн від попередньої дози ще діє) → `overdoseUntil` на 4с
(`OVERDOSE_DURATION_MS`), стрес +25 (`OVERDOSE_STRESS_BUMP`), без смерті
(за вибором користувача). Ефект: тремор ТІЛА мухи (окремий jitter на
позиції відрисовки, відмінний від shakeX/shakeY — той трясе весь стіл)
+ тимчасова втрата контролю ходьби (випадковий вектор додається до
рішення руху щотакту, поки триває). Перевірено живо: друга випита кава
під час дії кофеїну коректно виставляє overdoseUntil на +4000мс і
стрес +22 (з природним спадом).

Заодно перевірив і підтвердив: `CAFFEINE_SPEED_MULT` (стимулятор —
швидкість) уже підключений і реально впливає на `groundSpeed()`.

---

### ✅ КРИТИЧНИЙ БАГ: "стає на їжу й дрейфує геть" — tryMoveGround() (2026-09-17)

Користувач повідомив: муха підходить до їжі, "зупиняється", але видимо
рухається на місці, не їсть, іде до яблука й повертається назад.

**Корінь:** `tryMoveGround()` викликається БЕЗУМОВНО щотакту для БУДЬ-ЯКОГО
стану (walk/atfood/groom/idle/sleep), використовуючи ПОТОЧНІ vx/vy. Коли
муха "застигає" (atfood/groom/idle/sleep явно ставлять `vx=0;vy=0;`),
`nx=pos.x+0` тривіально дорівнює `pos.x` — перша умова (`nx!==pos.x`)
хибна не тому, що рух заблоковано, а тому, що руху взагалі не просили.
Код цього не розрізняв і щотакту провалювався аж до RANDOM WALK
fallback (в кінці функції) — той не лише РЕАЛЬНО зсував позицію на
випадкову дельту, а й ПЕРЕЗАПИСУВАВ vx/vy цим випадковим вектором, який
потім PERSIST'ИВ (нічого більше його не скидає, поки стан лишається
'atfood' і т.п.) — тобто щойно "застигла" муха миттєво починала
дрейфувати в довільному напрямку й продовжувала дрейфувати весь час
"їжі", часто не встигаючи дійти до фактичного moment "їсти" або
відходячи від їжі геть.

**Перевірено живо (до фіксу):** `vx=vy=0`, `state='atfood'`, 10 викликів
`tryMoveGround()` поспіль — позиція монотонно "втекла" на ~1.6px БЕЗ
жодного зовнішнього запиту руху, vx/vy застрягли на випадковому
ненульовому векторі. **Фікс:** один рядок на початку функції —
`if(vx===0 && vy===0) return;` (нуль-вектор = "не рухати взагалі", не
"рух заблоковано"). **Перевірено після фіксу:** та сама послідовність —
позиція лишається РІВНО незмінною всі 10 викликів; повний цикл
підхід-до-їжі→atfood→з'їла→пішла далі — позиція заморожена ВСЮ atfood-
фазу (раніше дрейфувала), голод коректно скидається, звичайна ходьба
(vx/vy ненульові, впирається в перешкоду → random-walk fallback) не
зачеплена.

**Це, найімовірніше, СТАРИЙ баг** (сама функція не змінювалась цієї
сесії) — просто раніше не був настільки помітний. Не виключено, що
частота "застигань" (idle/atfood/sleep) зросла з появою sleep/сієсти
цієї ж сесії, зробивши баг видимішим.

**Погоджено з користувачем: варто провести окремий, зосереджений раунд
регресійного тестування** — за одну сесію сьогодні змінено дуже багато
взаємопов'язаних систем (dust/groom, смаки, спрага, сенсорний джитер,
фонове світло, оптичний потік, віяло променів VIS_LO, сон, кава) —
ризик прихованих взаємодій, подібних до щойно знайденої, реальний.

### ✅ Регресійний прогін після знахідки tryMoveGround() (2026-09-17)

**Знайдено й виправлено ще ОДИН баг того ж класу** одразу при
регресійному тестуванні: `groom`/`idle`/`sleep` при вході в стан НЕ
скидали vx/vy (на відміну від atfood, де це вже робилось) — залишкова
швидкість із ходьби ДО стану й далі застосовувалась `tryMoveGround()`,
тож "застигла" на грумінг/відпочинок/сон муха тихо КОВЗАЛА в напрямку,
куди щойно йшла. Перевірено живо (`vx=0.3,vy=-0.2`, 15 викликів
`tryMoveGround()` без фіксу): усі три стани дрейфували на ~5.4px.
Фікс: `vx=0; vy=0;` додано в усі три місця входу в стан (той самий
патерн, що вже був для atfood). Перевірено через РЕАЛЬНІ тригери
(не ручне присвоєння стану) для idle й sleep — вхід у стан коректно
обнулює vx/vy; groom підтверджено інспекцією коду (той самий однорядковий
патерн, ідентичний перевіреним idle/sleep).

**Пройдено систематично, усе живими прогонами:**
- **Яблуко** — застигає СПРАВДІ нерухомо весь atfood (раніше дрейфувала), голод 95→0, спрага падає (THIRST_RELIEF_APPLE).
- **Салямі** — застигає нерухомо, голод→0, спрага РОСТЕ (THIRST_PENALTY_SALAMI).
- **Кава** — застигає нерухомо, спрага падає (THIRST_RELIEF_COFFEE), caffeineUntil виставляється; на ЧИСТОМУ перезавантаженні жодних від'ємних значень (попередня "негативна спрага" — знову підтверджено, це забруднення стану з попередніх тестів у тій самій сесії, не реальний баг — той самий висновок, що й раніше цієї сесії).
- **Сон** — нічний вхід за реальним тригером, `sleepDebt` ніколи не йде в мінус, природне пробудження, тепер ще й коректно нерухомий (щойно виправлений фікс).
- **"Складніше розбудити"** — підтверджено ще раз: слабкий струс не будить, сильний — будить.
- **Провід (біль/навчання)** — дотик коректно дає `wirePunishedThisTick=true`, миттєвий переляк (fear 0→0.55), зліт, природний спад страху, `kcWireIndices` будується — контур навчання (Фаза 2, цієї сесії не чіпався) не зачеплений жодною зі сьогоднішніх змін.
- **Вільний прогін** 80 тактів (~20с ігрового часу) без жодного форсування — 0 помилок у консолі, 0 винятків, стабільна поведінка.

Ширші області (сенсорний джитер/VIS_R1R6, фонове світло, оптичний потік,
віяло променів VIS_LO) вже мали живу перевірку в момент впровадження
(вище в цьому документі) — тут лише підтверджено відсутність нових
конфліктів із щойно знайденим і виправленим рухомим багом.

### ✅ "Зробити страх реальним" — реальний GNG_DESC-шлях (2026-09-17)

За прямою вказівкою користувача ("чи є відчуття страху... чому і як
зробити реальним") дослідив РЕАЛЬНІ зв'язки в `connectome.bin.gz`
(python-скрипт, читав бінарник напряму — `pre/post/weight` CSR-формат,
метадані group_id) від обох реальних "небезпечних" входів:

- **`OLF_ORN_DANGER`** (430) — прямо лише в `OLF_PN`/`OLF_LN` (звичайне
  перше реле нюху). За 2 кроки (через його 79 конкретних OLF_PN-цілей):
  найсильніше — `MB_KC` (2450 зв'язків), також `LH_APP` (925),
  `GNG_DESC` (166).
- **`GUS_GRN_BITTER`** (65) — прямо: здебільшого `GENERIC_CENTRAL` (487,
  некласифікований шум), і **`GNG_DESC` напряму (67 зв'язків, 1 крок)**.

**Критична знахідка:** перевірив, куди веде найсильніший шлях (`MB_KC`
→ ?) — **усі реальні виходи MB_KC йдуть ЛИШЕ в `MB_MBON_APP`(21438)/
`MB_DAN_REW`(12225)** — апетитивні/нагорода, той самий шлях, що й
солодкий смак! Жодного реального зв'язку з аверсією, бо `MB_MBON_AV`/
`MB_DAN_PUN` фізично не існують (0 нейронів) в цих даних. Отже
стимулювати MB_KC "від небезпеки" було б архітектурно нечесно —
переплутало б "смачно" й "небезпечно" в тому самому реальному контурі.

**Висновок: `GNG_DESC` (3581 живих, головний низхідний вихід мозку) —
ЄДИНИЙ реальний, непорожній, valence-нейтральний шлях від ОБОХ
небезпечних входів**, і анатомічно осмислений (буквально "як мозок
каже тілу рухатись", байдуже до емоційного забарвлення).

**Реалізовано:**
- `gngDescFearIndices` — фіксована підмножина 100 із 3581 реальних
  нейронів `GNG_DESC` (той самий перевірений патерн `stimulateIndices`,
  що вже для kcHot/kcWire/kcRoach), зріз з КІНЦЯ діапазону (не
  перетинається з іншими підсистемами).
- `stimulateFearGngDesc(intensity)` — одноразова пряма ін'єкція, з
  `SLEEP_AROUSAL_DAMPING` (той самий сенсорний гейтинг уві сні).
- "Липкий" акумулятор `gngDescFearMaxCountSinceCheck`
  (`GNG_DESC_FEAR_POLL_MS=60`) — той самий патерн, що й kcHotFiredSinceCheck
  (щоб не пропустити коротке спрацювання між тактами мозку), читається
  й скидається раз на такт (`BRAIN._fearSubsetActivity`).
- **`js/brain-worker-bridge.js`, `synthesizeMotorOutputs()`:**
  `flightIntent` тепер має РЕАЛЬНИЙ, а не лише synthetic, внесок:
  `+ fearGngDescActivity * 1.2` (раніше всі 5 термів flightIntent —
  dFear/mbAv/lhAv/dnStartle/noci — читали групи з 0 живих нейронів,
  тобто flightIntent був на 100% ігровим скаляром).
- Підключено в УСІХ реальних тригерах страху: дотик проводу (3.0,
  найсильніший — реальний біль), дотик таргана (2.5), сильний/слабкий
  струс (2.5/1.5 × arousalScale), клік (1.0 × arousalScale), і всі 3
  ВИВЧЕНІ CS-реакції (чашка/дріт/тарган, пропорційно вивченій вазі).

**Перевірено живо:** дотик проводу — `gngDescFearIndices` дійсно
будується (100 нейронів), реальна LIF-активність у підмножині
спостерігається (пік 82/100 одразу після болю, спадає до фонових 4-8
за кілька тактів) — справжня, вимірювана мережева відповідь, не
формула. Сон-гейтинг і далі коректно розрізняє слабкий струс (не
будить) і сильний (будить) — новий реальний шлях цього не зламав.
Звичайний клік наяву — та сама поведінка (зліт, fear≈0.15), без
регресії. Чистий прогін 40 тактів після цього — 0 нових помилок.

**Чесна межа, що лишається:** `BRAIN.drives.fear` (скалярне
"довго боюсь", накопичення/спад) НЕ замінено — це свідомо (гормональний
стан, не нейронне обчислення саме по собі). І оскільки `stimulateFearGngDesc`
викликається з `onCanvasClick()`/`shakeTable()` (одноразові
`BRAIN.update()`-виклики, не через `runBrainTick()`), реальний внесок
`BRAIN._fearSubsetActivity` читається лише НАСТУПНИМ такту мозку — той
самий відомий лаг одноразових `BRAIN.update()`-викликів, що й раніше
задокументований для reward/дофаміну; не втрачається, лише трохи
запізнюється.

### ✅ Спрага-яблуко, тривалість грумінгу, діагональна анімація, "думки" (2026-09-17)

**Баг: спрага 100% ігнорувала яблуко (і, здавалось, каву).** Кава сама
по собі перевірена ізольовано — працює. Яблуко — підтверджений баг:
`findNearestFoodInRange()` досі вважав "водою" лише каву
(`isWater = f.type==='coffee'`) — застарілий коментар із часів ДО появи
`THIRST_RELIEF_APPLE` (яблуко теж гасить спрагу, доданий раніше цієї ж
сесії). Фікс: `isWater = coffee || apple`; і `feedReady` тепер дозволяє
"спрагла-але-не-голодна" рішення їсти і для яблука (не лише кави).
Перевірено живо: спрага 100%, голод низький — тепер іде до яблука,
їсть, спрага падає (98→48, точно THIRST_RELIEF_APPLE=50).

**Тривалість грумінгу → 5с.** Було лише 780мс (GROOM_FRAME_MS×
GROOM_TOTAL_FRAMES=130×6) — замало для "протирання". Розділив
тривалість СТАНУ (`GROOM_DURATION_MS=5000`) від довжини ОДНОГО проходу
анімації (6 кадрів) — тепер кадри циклічно ПОВТОРЮЮТЬСЯ
(`frameIndex=(frameIndex+1)%GROOM_TOTAL_FRAMES`, було
`Math.min(...,GROOM_TOTAL_FRAMES-1)` — заморожувалось на останньому)
на всі 5 секунд.

**Реальний баг анімації по діагоналях.** `DIAG_ANGLE_OPP` (121.2°) і
`DIAG2_ANGLE` (119.5°) відрізняються лише на 1.7°, обидва в межах
DIAG_TOLERANCE (~20°) один від одного — а перевірка йшла по черзі
(if/else if), тож `DIAG_ANGLE_OPP` (перевіряється РАНІШЕ) завжди
"перехоплював" напрямок, призначений для `DIAG2_ANGLE`: другий
діагональний аркуш (`walkDiag2Frames`) практично НІКОЛИ не обирався,
навіть рухаючись точно в його напрямку (той самий конфлікт дзеркально
з іншого боку: `DIAG_ANGLE`=-58.8° проти `DIAG2_ANGLE_OPP`=-60.5°).
Фікс: замість порядку перевірки — шукаємо НАЙБЛИЖЧИЙ із 4 еталонних
кутів (мінімальна різниця). Перевірено живо (перехоплено реальні
`ctx.drawImage()`-виклики): рух точно під `DIAG2_ANGLE` тепер коректно
дає `diagMode=2`, малює з `walkDiag2Frames` (раніше — `diagMode=1`,
`walkDiagFrames`, неправильний аркуш).

**Попутна знахідка (defensive fix, НЕ підтверджено як реально досяжний
у звичайній грі):** на vertical/pcside поверхнях idle/sleep навмисно НЕ
скидають `frameIndex` (стоп-кадр останньої пози лазіння), але грумінг
має 6 кадрів (0-5), а `climbFrames` — лише 5 (0-4): якщо frameIndex
залишається 5 і потрапляє в цю гілку — `climbFrames[5]` невизначений,
рендер падає. Живе відтворення показало, що груминг-вихід ЗАВЖДИ сам
скидає frameIndex=0 раніше, тож цей конкретний ланцюжок, схоже,
насправді недосяжний у нормальній грі — але клемп (`Math.min(frameIndex,
climbFrames.length-1)` замість "не чіпати") доданий як безкоштовний
захист про всяк випадок, без зміни жодної коректної поведінки.

**"Думки" — за прямою вказівкою користувача.** Новий рядок `#thought`
під канвасом (окремо від технічного `#status`, який показує сирі назви
активних груп нейронів) — людяне зведення поточного наміру:
"Вдарило струмом!", "Тікає в паніці!", "Спить"/"Дрімає (сієста)",
"Чистить лапки від пилу", "їсть"/"П'є каву", "Відпочиває", "Насторожена
таргана", "У пошуках води"/"У пошуках їжі", "Досліджує територію",
"Гуляє". `computeThought(t)` читає ВЖЕ існуючий стан (state/mode/
brain.*/chaseFood/roach/overdoseUntil/lastPainAt) — не нова нейронна
механіка, лише пріоритетний UI-переклад. Перевірено живо для кожного
сценарію окремо (чисте налаштування без застарілого stateUntil з
попередніх тестів — та сама пастка забруднення стану, що вже
траплялась цієї сесії).
