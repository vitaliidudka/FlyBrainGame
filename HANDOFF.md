# Fly Pixel Game — передача для підключення мозку (Claude Code)

## 🧭 Почни звідси — огляд для нового чату (стан на 2026-09-15)

**Що це, в двох реченнях.** Один self-contained HTML-файл
(`fly-pixel-game.html`, ~4.8МБ, усі спрайти вшиті base64) — 2D pixel-art
симуляція мухи на робочому столі. Усі рішення мухи (куди йти, коли
їсти/чиститись/летіти/боятись/навчатись уникати небезпеку) виходять із
СПРАВЖНЬОГО нейронного конектома (клон [`snedea/flybrain`](https://github.com/snedea/flybrain),
~70 лумпованих груп нейронів), а не з `Math.random()` — щодо
`Math.random()`, що лишився, дивись нижче "Що НЕ реалізовано" й самі
коментарі `[BRAIN]` у коді.

**Як запустити.** Ніякої збірки. `python3 -m http.server 8080` у цій
теці → `http://localhost:8080/fly-pixel-game.html`, або просто відкрити
файл напряму в браузері (`file://` працює, крім хіба debug-серверних
речей). Детальніше — `README.md`.

**Файли репозиторію:**
- `fly-pixel-game.html` — уся гра, єдиний файл, що деплоїться
- `assets/*.png` — вихідні спрайт-аркуші (довідково, у рантаймі не читаються)
- `README.md` — коротко для GitHub-відвідувача, як запустити/задеплоїти
- `vercel.json` — маршрутизація деплою (`/` → `fly-pixel-game.html`)
- `HANDOFF.md` (цей файл) — повний технічний лог розробки, сесія за
  сесією, включно з причинами кожного рішення й знайденими багами.
  **Дорожня карта нижче** — чекліст, що реалізовано/заплановано.

**Архітектура за 30 секунд:** три шари — мозок (конектом, `BRAIN.*`) →
тіло (анімації/фізика/колізії) → сцена (фон/зони/сутності). Детальніше в
розділі "Архітектура" одразу нижче, і в кожному хронологічному розділі
далі у файлі.

**Методологія тестування, яку варто знати перед будь-якими змінами:**
змін перевіряються headless-браузером (не лише читанням коду) — драйвиться
`loop(t)` синтетичним годинником напряму з консолі/JS-виконання, а НЕ
справжнім `requestAnimationFrame`. ВАЖЛИВО: якщо повторно тестуєш на вже
завантаженій сторінці (не перезавантажуючи), обов'язково скинь
`lastHungerTick`/`lastBrainTick`/`nextPoopAt` до свіжого `tNow` на
початку кожного нового прогону — інакше застарілі таймери з попереднього
прогону дають катастрофічно неправильні результати (задокументовано як
знайдений і вирішений артефакт методології нижче у файлі).

**Поточний фокус (2026-09-15):** підготовка до деплою на GitHub + Vercel
(мобільна адаптація вже додана в код; `vercel.json`/`README.md`/
`.gitignore` підготовлені; git-репозиторій і сам деплой — наступний крок,
див. "Деплой" у дорожній карті нижче).

**Стратегія на майбутнє (сказано користувачем, не вигадано):** ЦЕЙ
репозиторій — "легка" версія (~70 груп нейронів) і лишається окремою
продакшн-версією. Паралельно, окремим проєктом, планується повна модель
на всіх ~130k+ нейронах (FlyWire) з реал-тайм візуалізацією активності
нейронів, на окремому хостингу — вона НЕ замінює цю версію, а існує поруч.

## 🗺️ Дорожня карта / історія фіч

Чекліст — `[x]` реалізовано (з датою сесії, де знайти деталі — шукай
відповідний заголовок нижче у файлі), `[ ]` заплановано/не почато.

**Ядро / мозок**
- [x] snedea/flybrain ~70-групового конектома підключено (`dendriteAccumulate`
  → `fireNeuron` → `runconnectome`, драйви hunger/fear/fatigue/curiosity/groom)
- [x] Усі первісні точки `[BRAIN]` (рандом/таймери) замінені реальними нейронами
- [x] Статус-рядок показує реальні нейрони, що спрацювали цього такту
  (`BRAIN.firedThisTick`), а не текстовий опис дії
- [ ] Повна ~130k+-нейронна модель (FlyWire) — окремий паралельний проєкт/
  хостинг, НЕ в цьому репозиторії (див. "Стратегія на майбутнє" вище)
- [ ] Реал-тайм візуалізація роботи/зв'язків усіх нейронів — там само,
  окремий проєкт

**Фізіологія**
- [x] Голод: фазова модель (харчова кома → перетравлення → гострий голод)
- [x] Дофамін / стрес: 0-100 з експоненційним згасанням
- [x] Втома польоту / відновлення на землі, гравітація змушує сісти на нулі
- [x] Фототаксис: два реальних джерела світла різної сили тяжіння
- [x] Повна термальна карта сцени (кожен об'єкт окремо, день/ніч)
- [ ] Спрага (`DRIVE_THIRST`, окремий індикатор від голоду) — після появи
  джерела чистої води (кавова пляна поки лише частково впливає на голод)

**Сцена й фізика**
- [x] Растрова карта зон (walk/hot/flight/floor/pcside/vertical) за кольором
- [x] Вільне повзання між суміжними наземними/лазильними зонами без зльоту
- [x] Лазіння по вертикальних поверхнях
- [x] Автономний зліт/приземлення (без кнопок), реальна гравітація
- [x] День/ніч перемикач, що реально впливає на термальну карту й світло

**Сутності на столі**
- [x] Їжа (салямі/яблуко): запах → переслідування → поїдання →
  `GUS_GRN_SWEET → MB_DAN_REW` (реальна дофамінова нагорода)
- [x] Какульки (`poop`)
- [x] Тарган: без мозку/AI, чисто випадковий рух, з'являється після
  першого "Кинути їжу" з затримкою
- [x] Тарган → зір/дотик/втеча мухи: `VIS_LO`/`VIS_LC`/`DN_STARTLE`/
  реально задіяний дотиковий шлях `BRAIN.stimulate.touch`
- [x] Оголений провід: візуальне розміщення (color-landmark matching) +
  реальна больова логіка (`NOCI`, SEE/TOUCH тіри, форсований зліт)
- [x] Кавова пляма: з'являється при струсі столу, вода+кофеїн,
  `GUS_GRN_WATER` вперше реально активний

**Навчання (дофамін-гейтована асоціативна пластичність)**
- [x] Чашка: `MB_KC_HOT` + `THERMO_MUG → MB_DAN_PUN`, вага
  `MB_KC_HOT→MB_MBON_AV` реально росте в рантаймі
- [x] Провід: `MB_KC_WIRE` + `VIS_WIRE`/`NOCI → MB_DAN_PUN`, з нуля
  (жодної вродженої аверсії, класична павловська схема)
- [x] Кофеїн прискорює навчання (`×1.5` на `MB_PLASTICITY_RATE`, поки діє)
- [ ] Тарган (`MB_KC_ROACH`) — навмисно ОСТАННІЙ у черзі; користувач
  прямо просив нагадувати про це, якщо він сам забуде й попросить раніше
- [ ] Розділення джерел `VIS_LC` (зараз стіна/тарган/клік — спільний
  нейрон) — обов'язкова передумова для навчання таргана, інакше страх
  хибно узагальниться на звичайні стіни

**Деплой (GitHub + Vercel, у процесі)**
- [x] Мобільна адаптація: `viewport` meta, responsive CSS (canvas
  масштабується під ширину екрана, внутрішня роздільність гри 380×680
  не змінюється — координати кліку/тапу вже й так перераховувались
  через `canvas.width/rect.width`, тому нічого не зламалось)
- [x] `vercel.json` (маршрут `/` → `fly-pixel-game.html`), `README.md`,
  `.gitignore` підготовлені
- [x] `git init` + перший комміт, GitHub-репозиторій підключено
  (`github.com/vitaliidudka/FlyBrainGame`), Vercel імпортовано
- [x] Мобільний фікс: `canvas { touch-action:none }` блокував свайп-скрол
  до контролів/індикаторів нижче канвасу (на мобільному він займає майже
  весь екран, тож свайп по ньому браузер повністю ігнорував) — змінено на
  `touch-action:pan-y`: вертикальний скрол тепер проходить, pinch-zoom і
  горизонтальний пан і далі заблоковані; повноцінна мобільна верстка
  (щоб усе влізало без скролу) — окремий майбутній крок
- [x] Виправлено email автора комітів: перші 2 коміти пішли з
  автозгенерованого `vitaliidudka@Vitaliis-Laptop.local` (git не мав
  налаштованого `user.email`) — Vercel заблокував деплой повідомленням
  "commit author email is not valid, ensure your git email matches your
  GitHub account". Виправлено `git config user.email/user.name` ЛОКАЛЬНО
  в цьому репозиторії (не глобально) на реальний email користувача; ЦЕЙ
  комміт — перший із правильним автором. Історія НЕ переписувалась
  (`git commit --amend`/rebase + force-push на `main` свідомо не робив —
  це деструктивна операція, яку не варто виконувати без прямого прохання
  саме на цю дію) — якщо Vercel досі блокує деплой через СТАРІ коміти в
  історії (а не лише останній), доведеться або переписати історію
  (force-push), або видалити й перестворити репозиторій наново.

## Що це

Прототип 2D pixel-art симуляції мухи на робочому столі (`fly-pixel-game.html`,
self-contained, спрайти вшиті base64). Зараз усі рішення мухи (куди йти, коли
чиститись, коли летіти, коли какати, реакція на їжу/світло) керуються простим
рандомом і таймерами. Мета наступного етапу — підключити реальний конектом
(`snedea/flybrain` або подібний) як джерело цих рішень замість `Math.random()`.

## Архітектура (з самого початку розмови)

Три шари, розділені навмисно:

1. **Мозок (поки заглушка)** — приймає рішення: куди рухатись, коли
   зупинитись/почистити лапки/полетіти/какати/їсти. Зараз це прості таймери +
   `Math.random()` всередині `loop()`.
2. **Тіло** — анімації (walk/idle/groom/flight/діагональні варіанти ходьби),
   фізика (гравітація, втома), колізії з растровою картою зон.
3. **Сцена** — фон стола, зони (walk/hot/flight/floor), їжа, світло, какульки.

Коли підключатимете мозок, замінюється **тільки шар 1**: замість
`Math.random()` — вихідна активність моторних нейронів визначає ту саму
множину рішень. Шар "тіла" лишається майже без змін.

## Головний конвенційний тег: `[BRAIN]`

У коді (`fly-pixel-game.html`) кожне місце, де зараз стоїть `Math.random()` чи
фіксований таймер замість майбутнього рішення мозку, позначено коментарем
`// [BRAIN] ...`. Пошук по файлу за рядком `[BRAIN]` дає повний список точок
для заміни. Приблизний перелік (див. самі коментарі в коді для деталей):

- коли какати (`nextPoopAt`)
- коли передумати напрямок ходьби / куди саме йти
- йти далі чи зупинитись відпочити (idle), як довго відпочивати
- куди піти після чистки лапок / після відпочинку
- скільки застигати біля їжі (`atfood`), і рішення "їсти чи ні" після паузи
- "не голодна найближчим часом" — cooldown конкретної їжі
- "не можу дістатись до їжі" — тайм-аут переслідування (6с)
- нижче якого рівня голоду запах їжі ігнорується (`HUNGER_SEEK_THRESHOLD`)
- швидкості голоду по фазах (кома/перетравлення/гострий голод)
- скільки дофаміну/стресу від їжі/какання/струсу столу, швидкості згасання
- швидкість втоми в польоті / відновлення на землі (`FLIGHT_STAMINA_*`)
- імовірність піддатись світлу (фототаксис) замість випадкового напрямку

## Фізіологічні системи (вже реалізовані як заглушки)

- **Голод** (`hunger`, 0-100): фазова модель — "харчова кома" → повільне
  спорожнення зоба → гострий голод. Секунди стиснуті версії реальних
  20хв/2-4год/12год (за матеріалами, які надав користувач).
- **Дофамін / Стрес**: прості 0-100 з експоненційним згасанням. Дофамін
  зростає від їжі й какання, стрес — від струсу столу.
- **Витривалість польоту** (`flightStamina`): витрачається в повітрі,
  відновлюється на землі. При 0% гравітація перемагає й змушує сісти.
- **Фототаксис**: два реальних джерела світла (координати й кольори виміряні
  з фото стола) притягують муху з різною силою (вікно сильніше за екран).

## Калібрувальні дані спрайтів (важливо для будь-яких змін анімації)

Кожен спрайт-аркуш має свою специфіку нарізки — **не всі кадри рівномірні
за шириною**. Дивіться коментарі біля `walkSheet`, `idleSheet`,
`groomFramesA`, `walkDiagFrames`, `walkDiag2Frames`, `flightFrames` у коді:
для кожного там або спільний масштаб на весь аркуш (walk/idle — рівномірні
кадри), або індивідуальний масштаб+анкор на кожен кадр (groom/діагоналі —
дуже нерівномірні через рух лапок/крил). Ці межі виведені аналізом
альфа-каналу вихідних PNG (шукали проміжки між фігурами), а не рівним
поділом — рівний поділ кілька разів спричиняв баги "протікання" кадрів.

Для flight-кадрів є додатковий нюанс: НЕ рахуйте масштаб окремо на кожен
кадр за bodyH, коли крила міняють форму (не висоту) — це "роздуває" тіло
візуально. Використано єдиний спільний масштаб (`FLIGHT_REF_BODY_H`) для
всього аркуша, з індивідуальними лише центрами позиції.

## Файли в цьому пакеті

- `fly-pixel-game.html` — основний файл гри
- `assets/*.png` — усі спрайти й растрова карта зон, витягнуті окремо
  (walk, idle, groom_handwash, walk_diag1, walk_diag2, flight, food_salami,
  food_apple, zone_mask, desk_scene). `groom_selfwash_unused.png` — аркуш,
  який був спробуваний для грумінгу, але прибраний за проханням користувача
  (лишився в коді як невикористаний завантажений спрайт).

## Мозок підключено: snedea/flybrain

Усі точки `[BRAIN]` у коді замінені на значення з реального ~70-групового
вагового конектома snedea/flybrain (MIT, дослівно перенесені `js/constants.js`
+ "легасі" шлях `js/connectome.js` того проєкту — dendriteAccumulate →
fireNeuron → runconnectome, ті самі драйви hunger/fear/fatigue/curiosity/groom).
Такт мозку — 500мс (`runBrainTick`, той самий інтервал, що й в оригіналі).
Деталі й обґрунтування кожної заміни — у коментарях біля кожного `[BRAIN]` в
`fly-pixel-game.html`.

Свідомо НЕ підключено `js/brain-worker-bridge.js` + `js/sim-worker.js` +
`data/connectome.bin.gz` (повна 139k-нейронна модель через Web Worker і
~12МБ бінарних даних FlyWire) — цей шлях вимагає HTTP-сервера (fetch +
`new Worker()` не працюють при відкритті файлу напряму як `file://`), а гра
свідомо self-contained (як і спрайти — вшиті base64). "Легасі" 59(~70)-групова
модель — той самий справжній конектом-двигун проєкту, просто синхронний і без
зовнішніх залежностей.

### Автономна поведінка (без кнопок "Політ"/"Приземлитись")

- **Пил на лапках** (`dust`, 0-100%): фоновий осад + випадкові порохові
  пориви (зовнішній подразник, лишається на `Math.random()`, як і тип
  впалої їжі). Стимулює `DRIVE_GROOM` напряму (НЕ `MECH_BRISTLE` — той
  одразу несе сильний `DN_STARTLE`/`DRIVE_FEAR`, це підходить для різкого
  дотику типу клік/струс, але не для хронічної сверблячки). Коли
  `accumGroom` конектома перетинає поріг — муха сама зупиняється й чистить
  лапки, дуст скидається.
- **Зліт/приземлення повністю автономні** (`runBrainTick`, наприкінці):
  сильний `accumFlight` (реальний вихід `DN_FLIGHT`/крила — втеча від
  страху/болю/спеки) = втеча; спокійна висока `DRIVE_CURIOSITY` (поріг 0.55
  — 0.75 виявився практично недосяжним при типовій динаміці дрейфу) без
  їжі поруч і без втоми = дослідницький переліт в іншу зону столу.
  Приземлення — коли `accumFlight`/`DRIVE_FEAR` спадають (заспокоїлась).

  **Дві додаткові знахідки, які раніше майже повністю блокували дослідницький
  політ (виявлені живою симуляцією 300с, коли скарга була "майже ніколи не
  літає сама"):**
  1. `DRIVE_CURIOSITY` (`BRAIN.updateDrives`) — чистий симетричний
     випадковий блукач без відновлювальної сили. Без неї він міг випадково
     "зайти" в низьку смугу й застрягти там (один прогін: дрейф 0.49→0.10 за
     300с, жодного разу не перетнув поріг 0.55). Додано м'яке повернення до
     базового 0.5 (`(0.5 - d.curiosity) * 0.003` щотакту) — досить слабко,
     щоб не прибрати випадковість, досить сильно, щоб не застрягати на
     сотні секунд у крайніх значеннях.
  2. Ґейт `exploreUrge` вимагав `fatigue < 0.4`, а власний поріг гри на
     "йти відпочити" — `restFatigue = 0.7`. Цикл ходьба/відпочинок природно
     ВРІВНОВАЖУЄ `fatigue` близько 0.7 (відпочиває лише щойно перетне цей
     поріг, трохи знижує й одразу веде назад до нього) — тобто стаціонарне
     значення в довгій грі завжди близько до 0.7, а не якогось "типового"
     низького рівня. Між 0.4 і 0.7 муха була вже недостатньо бадьора, щоб
     полетіти досліджувати, але ще недостатньо втомлена, щоб свідомо
     відпочити — "мертва зона", де дослідницький політ був заблокований
     майже увесь час (перевірено: 9792 з ~9850 придатних тактів блоковані
     втомою). Поріг підняли до `fatigue < 0.8`.

  Заміряно 5 незалежних чистих 300с прогонів (кожен — свіже завантаження
  сторінки, без залишкового стану): **5, 8, 13, 16, 0** автономних
  дослідницьких зльотів. 4 з 5 перевищують ціль "3-5 разів мінімум"; один
  прогін дав 0 — це притаманна модель стохастичність (символьний випадковий
  блукач іноді просто не має "сплеску" цікавості за 300с), а не залишковий
  баг.
- **Клік по мусі — тінь, що лякає** (не скрипт "почисти лапки"): напряму
  пушить `VIS_LC` (loom-детектор) і `DN_STARTLE` (гігантське волокно
  ескейп-рефлексу Drosophila — у реальних мухах ця ланка майже завжди
  спрацьовує одним різким тригером на такий стимул), одразу прораховує
  такт конектома — реакція (зазвичай втеча) видима негайно.
- **Температура**: реальне термальне поле навколо гарячої чашки
  (`findHeatLevel`, обчислюється скануванням растрової 'hot'-зони при
  старті) годує `BRAIN.stimulate.temperature` → `THERMO_WARM` → відштовхує
  ще до фізичного зіткнення з межею зони (яка й так непрохідна).

### Світло: що воно РЕАЛЬНО впливає, а що ні

`findLightPull()` вибирає **одне найсильніше зважене джерело в радіусі**
(`strength * (1 - d/radius)`, найбільше значення перемагає) — НЕ усереднює
вектори кількох джерел одразу (стара версія так робила й давала "перетин
зон", де муха застрягала між монітором і вікном, бо усереднений вектор не
вів чітко нікуди). Якщо десь є ОБ'ЄКТИВНО сильніше джерело (за стелею
`strength`) навіть ПОЗА його формальним радіусом — і різниця істотна
(`LIGHT_OVERRIDE_MARGIN = 0.2`) — муха "переманюється" туди: без цього
вікно (стеля 0.75) фізично не могло конкурувати з монітором (стеля 0.4),
щойно муха опинялась у монітор­ному радіусі (110px), бо відстань між
джерелами ≈195px — завжди поза 170px радіусом вікна.

`BEHAVIOR_THRESHOLDS.phototaxisLight` виправлено з `0.5` на `0.25` — старе
значення було бездумно перенесене з `js/fly-logic.js` flybrain, де
`lightLevel` — глобальний перемикач Bright/Dim/Dark (0 / 0.5 / 1), геть
інша шкала. У нашій моделі (спад за відстанню від конкретного джерела)
стеля монітора — `0.4` — була **фізично недосяжна** для порогу `0.5` за
жодної відстані: перевірено живою симуляцією (`monitorMaxLevelReachableInGame
= 0.4 < 0.5`), тобто монітор НІКОЛИ не міг запустити свідомий фототаксис.

**Сенсорна габітуація (звикання).** Перша версія override-логіки (вище)
розв'язала "монітор недосяжний", але створила ДЗЕРКАЛЬНУ проблему: щойно
муха діставалась вікна (стеля 0.75, радіус 170px — величезна частка мапи),
воно ставало майже незворотною пасткою (перевірено: 577-584 із 600с у
радіусі вікна). Причина — фототаксис-ґейт (`curiosity > 0.2`) виконується
майже завжди, і поки муха В МЕЖАХ сильного джерела, воно раз у раз
перемагає при кожному повторному виборі напрямку. Додано `lightHabituated`/
`lightEngagement`: якщо ОДНЕ Й ТЕ САМЕ джерело перемагає стабільно довше за
`LIGHT_HABITUATION_AFTER_MS` (20с) — воно на `LIGHT_HABITUATION_COOLDOWN_MS`
(25с) тимчасово "приїдається", і `findLightPull()` активно штовхає муху
геть від нього (не просто "перестає бачити" — пасивне ігнорування не встигало
вивести муху за межі 170px радіуса випадковим блуканням до закінчення
кулдауну).

**Світло НЕ впливає на дофамін/стрес — лише на вибір напрямку руху.**
`light.level` іде тільки в `BRAIN.stimulate.lightLevel`, звідти —
`VIS_R1R6`/`VIS_R7R8` (одразу в `BRAIN.update()`, дослівно з
`js/connectome.js`), які реально ведуть до `VIS_ME`/`LH_APP`/
`DRIVE_CURIOSITY` (нейрон, не скаляр) і опосередковано до `CX_EPG`
(орієнтація) — але **жодного шляху від VIS_* до `MB_DAN_REW`** (справжній
дофамінергічний нейрон, звідки береться ігровий `dopamine`) у вагах
flybrain немає. Найближче — `VIS_R7R8 → LH_APP` (innate appetitive,
"підхід", не винагорода) і пряме `VIS_R1R6 → DRIVE_CURIOSITY:2` (нейрон
цікавості, вже відпрацьовує сам собою через штатний `BRAIN.update()`, без
додаткового коду з нашого боку). Підключати світло до `dopamine`/`stress`
у грі було б вигаданою прив'язкою, не підтвердженою вагами конектома —
свідомо цього не робимо.

Перевірено симуляцією ~100с ігрового часу (прямий виклик `loop()` в обхід
`requestAnimationFrame`, бо фонова вкладка призупиняє rAF): пил надійно
провокує чищення лапок без випадкових злетів, дослідницькі перельоти
трапляються природно (~1 раз/20с), їжа й світло досі працюють коректно,
жодних помилок у консолі за тисячі кадрів.

### Орієнтація спрайту (виправлено)

Був системний баг: муха рендерилась "задки" майже для будь-якого руху (звичайна
ходьба, політ, дослідницький рух, фототаксис) — рухається вправо, а спрайт
малює голову вліво, і навпаки.

Гіпотеза "деякі нові джерела руху (світло/дослідження) забувають оновити
`facingLeft` після зміни `vx/vy`" НЕ підтвердилась: `facingLeft = vx<0` реально
викликається щокадру і в `moveAir()` (польот), і в ground-гілці `state==='walk'`
(після `tryMoveGround()`) — тобто охоплює всі джерела руху (ходьба, їжа,
фототаксис, `brainDir()`), бо це єдина точка, яка читає вже фінальний `vx` за
кадр, а не перевіряє кожне джерело окремо.

Справжня причина — одна переплутана полярність у рендері:
```js
const shouldFlip = useDiag ? diagFlip : !facingLeft; // БУЛО — неправильний знак
```
Базовий спрайт (`walkSheet`/`idleSheet`/`flightImg`) намальований ГОЛОВОЮ
ВПРАВО. `facingLeft=true` означає "рухається вліво" (`vx<0`) — отже саме
ТОДІ спрайт треба дзеркалити, а не навпаки. Підтверджено прямим ізольованим
рендер-тестом (без симуляції, напряму викликаючи код відмальовки з
контрольованими `facingLeft`/`vx`): при старій формулі рух вліво завжди
малював голову вправо. Виправлено на:
```js
const shouldFlip = useDiag ? diagFlip : facingLeft; // СТАЛО
```
Діагональна пара (`diagMode`/`diagFlip`, кути `DIAG_ANGLE`/`DIAG2_ANGLE`) —
НЕ займана: це окрема, свідомо змодельована система (дзеркало = "той самий
вектор, у інший бік", коментар у коді біля `DIAG_ANGLE_OPP`), перевірена
окремо й рендериться коректно як була.

Перевірено рендер-тестом (ізольовано від симуляції, щоб виключити шум
brain-тіків/фототаксису/зіткнень, які постійно перезаписують `vx/vy` під час
живого прогону) для: ходьба вліво/вправо, політ вліво/вправо, обидва
діагональні кути — і додатково живим прогоном (`vx=-0.3, facingLeft=true` під
час звичайної ходьби) — в усіх випадках голова тепер збігається з напрямком
руху.

### Зір: справжня стимуляція VIS_LO/VIS_LC/VIS_LPTC (не вигадана)

Три доповнення до реальних зорових контурів flybrain — усі три нейрони й ваги
вже існували в `constants.js` (`VIS_LO→VIS_LC:5/MB_KC:4/CX_EPG:3/LH_APP:2`,
`VIS_LC→DN_STARTLE:12/DRIVE_FEAR:6/DN_FLIGHT:5/GNG_DESC:3`,
`VIS_LPTC→CX_EPG:6/CX_HDELTA:5/CX_PFN:4/DN_TURN:3`), просто ніколи не
отримували прямого стимулу від гри. Нічого не вигадано понад вагову таблицю.

**1. "Периферійний" зір (VIS_LO) замість сліпого `blocked()`.**
`senseSurroundings()` пускає віяло з 8 променів (крок 20px, дальність 180px)
навколо позиції, `zoneAt()` вздовж кожного — знаходить найближчу межу
поточної зони. Результат годує `VIS_LO` ("object detection" у вагах) —
загальне "відчуваю структуру навколо". Побачена ІНША придатна зона
(walk/flight/floor, не сама поточна) додатково штовхає `DRIVE_CURIOSITY`
(та сама роль, що й `VIS_R1R6→DRIVE_CURIOSITY:2` у вагах — "visual novelty").

**2. Loom/зіткнення (VIS_LC) — окремий дрібний промінь лише вперед по курсу.**
`frontCollisionDist()` (крок 4px, дальність 12px) — точніший за периферійний
фан, де потрібна саме роздільність "впритул". Стимулюється ЛИШЕ коли близькість
тримається кілька тактів поспіль (`visCloseStreak`, дебаунс) — одноразового
"зачепила краєм" недостатньо, це має бути стійке "застрягла/тицяється".

**3. Оптичний потік (VIS_LPTC) замість константи 0.3.**
`brightnessAt()` порівнює яскравість "тут" і "трохи попереду по курсу" —
реальний градієнт замість вигаданого фіксованого сигналу. ВАЖЛИВО (чесно):
сам НАПРЯМОК повороту до світла й далі рахує зовнішня `findLightPull()` —
`CX_HDELTA` у цьому конектомі лише накопичувальний (немає гальмівних/
від'ємних входів, не може кодувати знак/бік — задокументована структурна межа
коннектома, не наша недоробка, див. розділ вище про фіксований баг
`brainDir()`). Тепер бодай МАГНІТУДА повороту (через `CX_HDELTA` у
`brainDir()`) реально залежить від того, що муха бачить, а не від константи.

**Складний момент калібрування (важливо для майбутнього тюнінгу):** у цьому
конектомі НЕМАЄ спаду (decay) для немоторних нейронів (`DRIVE_FEAR`/
`DN_FLIGHT`/`DN_STARTLE`/`VIS_LC` як графові нейрони) — `runconnectome()`
лише переносить стан, без віднімання, аж поки нейрон не перетне
`fireThreshold`(22). Перша версія стимуляції VIS_LC (проста дистанція <40px)
давала escape-зльотів 18-32/300с — набагато частіше за задум (мала бути рідкою
реакцією на СПРАВЖНЮ загрозу). Причина виявилась глибшою за просто "занадто
щедрий поріг": VIS_LO САМ має вагу 5 до VIS_LC, і навіть "м'яка" стимуляція
VIS_LO щотакту (на цій тісній захаращеній мапі щось близьке по одному з 8
променів — майже завжди) без жодного спаду в графі періодично розганяла VIS_LC
до порогу ОРГАНІЧНО (виміряно: 71-80 органічних спрацювань VIS_LC/300с — навіть
більше за пряму стимуляцію). Виправлено дебаунсом для прямого VIS_LC-стимулу
(streak, одноразовий імпульс на епізод застрягання) і зменшенням СЕРЕДНЬОЇ
сили VIS_LO (`*0.15`, бо на цій мапі важіль — не поріг спрацювання, а частота/
сила кожного імпульсу). Фінальні виміри (4 незалежні прогони 300с кожен, свіже
завантаження): escape-зльотів 9, 1, 10, 2 (було 18-32) — тепер рідкісна
реакція, як мало бути; дослідницьких зльотів 15, 6, 7, 1 — без регресії
відносно попередньої базової лінії (5-16). 0 виходів за межі canvas у всіх
прогонах. Фототаксис не зачеплений: стрімінг до світла й далі йде через
`findLightPull()` (незмінно), VIS_LPTC впливає лише на магнітуду випадкового
повороту, коли фототаксис НЕ активний.

### DRIVE_FATIGUE: єдине джерело, асиметричне ходьба/політ

Перевірка виявила два реальні баги:

1. **Втома вже росла і від ходьби, і від польоту — але ОДНАКОВО, без асиметрії.**
   `BRAIN.updateDrives()` нараховувала приріст за єдиною умовою
   `BRAIN._isMoving` (`mode==='air' || state==='walk'`), без розрізнення
   режиму. Тобто "лише від польоту" — невірне припущення, а "не нуль проти
   чогось" (як хотілось) — теж не виконувалось: обидва режими коштували
   рівно стільки ж.
2. **Гравітація/підйомна сила були прив'язані до ОКРЕМОГО лічильника**
   `flightStamina` (0-100, власний drain/recover-за-секунду), лише частково
   промодульованого `BRAIN.drives.fatigue` через множник швидкості зміни —
   не напряму. Два незалежних джерела "втоми" в грі одночасно.

**Виправлено:**
- `liftForce` (гравітація) тепер рахується НАПРЯМУ від `brain.fatigue`:
  `LIFT_FORCE_MAX * (1 - brain.fatigue)`. `flightStamina` та пов'язані
  константи (`FLIGHT_STAMINA_DRAIN_PER_SEC`/`RECOVER_PER_SEC`) видалені
  повністю. UI-бар "Витривалість польоту" тепер лише ІНШЕ представлення
  того самого `brain.fatigue` (`(1-brain.fatigue)*100`), не окремий рахунок.
- Термінова посадка (`urgentLanding` у `moveAir`) і поріг "спокою" перед
  приземленням (`calmNow` у `runBrainTick`) тепер теж читають `brain.fatigue`
  напряму (`URGENT_LANDING_FATIGUE=0.8`, `CALM_MAX_FATIGUE=0.85`), замість
  старих порогів по `flightStamina` (20%/15%).
- Додано клемп `d.fatigue` до 0-1 у `BRAIN.updateDrives()` — раніше не був
  потрібен (значення лише порівнювалось з порогом), тепер від нього напряму
  залежить фізика льоту, тож вихід за межі дав би від'ємну/надлишкову
  підйомну силу.
- Асиметрія ходьба/політ: старі "універсальні" коефіцієнти (0.003 світло /
  0.006 темрява) перепризначені саме ходьбі; політ отримав новий, значно
  вищий темп (~6.7×), щоб ефект справді був помітний у межах типової
  тривалості польоту, а не розтягувався на понад хвилину:

  | Режим | На світлі | У темряві |
  |---|---|---|
  | Ходьба (`FATIGUE_GAIN_WALK_*`) | 0.003/такт | 0.006/такт |
  | Політ (`FATIGUE_GAIN_AIR_*`) | 0.02/такт | 0.04/такт |

  (такт = 500мс, BRAIN_TICK_MS). Перевірено ізольовано (примусовий
  `mode='ground'`/`'air'`, один виклик `BRAIN.updateDrives()`): дельта точно
  відповідає таблиці.

- Швидкість польоту +10% (`FLIGHT_SPEED_MULT=1.1`), застосовано в
  `takeoff()` (усі сценарії зльоту) і в `moveAir()` (терміновий політ до
  посадки, фототаксис-у-повітрі, звичайний дослідницький переліт).

Регресія (2×300с, свіже завантаження, без ручних кліків/струсів): 0 помилок,
0 виходів за межі canvas, escape-зльотів 9-10, дослідницьких 4 — без
регресії відносно попередньої бази.

### DRIVE_FATIGUE: баланс відновлення, їжа, дофамін, голод-стрес

Чотири пов'язані правки поверх щойно уніфікованої системи (див. розділ вище).

**1. Швидкість відновлення на землі — прискорена втричі.**
Було: `d.fatigue -= 0.01`/такт (=0.02/с) — повне відновлення з 1.0 до 0
займало **50с**. Стало: база `FATIGUE_RECOVERY_RATE_BASE=0.03`/такт (=0.06/с,
**16.7с** до повного відновлення), додатково прискорюється дофаміном (п.4).
Асиметрія НАКОПИЧЕННЯ ходьба/політ не зачеплена — йдеться лише про спад.

**2. Їжа — одноразовий "укол" відновлення, не таймер.**
Обрано природніший з двох запропонованих варіантів: масштабований від
РЕАЛЬНОЇ сили `MB_DAN_REW` (той самий `reward`, що й дофамін поруч, той самий
момент), а не окремий таймер-множник — типове значення `reward≈7` дає
`~0.14` миттєвого полегшення `DRIVE_FATIGUE` (`FOOD_FATIGUE_RELIEF_SCALE=0.02`).
Не окремий механізм від п.4: їжа ще й підіймає дофамін, який далі сам
підсилює відновлення на час, поки не згасне (~25с).

**3. Голод на максимумі: дальність нюху + постійний стрес.**
- Дальність пошуку їжі (`SMELL_RADIUS`) раніше була ФІКСОВАНОЮ незалежно від
  рівня голоду — на порозі пошуку (20%) і на 100% радіус був однаковий,
  "відчайдушність" ніяк не підсилювала пошук. Тепер росте до +50% на 100%
  голоду (`effectiveSmellRadius()`, 150→225px).
- Новий постійний (не одноразовий) тиск стресу, поки голод вище 80%
  (локальна 0-100 шкала): наростає ПРОПОРЦІЙНО перевищенню порогу, швидкістю
  вищою за фоновий спад стресу (`HUNGER_STRESS_RATE_PER_SEC=100/12` проти
  `STRESS_DECAY_PER_SEC=100/18`) — тобто справді НАКОПИЧУЄТЬСЯ, поки голодна,
  а не просто повільніше згасає.

**4. Дофамін — безперервний модулятор відновлення, не подія.**
`updateDrives()` щотакту читає ЖИВЕ значення `dopamine` (0-100):
`dopamineMult = 1 + (dopamine/100)*0.5` — на максимумі дофаміну відновлення
+50% швидше. Не тригер, не таймер — просто завжди активний множник.

**Виміряно (а): час повного відновлення 0↔100% на землі:**

| | Старе | Нове (без дофаміну) | Нове (дофамін=100) |
|---|---|---|---|
| Швидкість | 0.02/с | 0.06/с | 0.09/с |
| Час повного відновлення | 50с | 16.7с | 11.1с |

**Виміряно (б): скільки коротких злетів поспіль (4с політ, 3с пауза на
землі між ними) до першої термінової посадки через виснаження** — 10 свіжих
прогонів кожен (окреме завантаження сторінки, щоб уникнути забрудненого
стану коннектома між прогонами):

| | Старе (10 прогонів) | Нове (8 успішних прогонів) |
|---|---|---|
| Середнє | ~7.7 злетів | ~12.5 злетів |
| Розкид | 3-14 | 5-28 |

~60% більше коротких злетів поспіль у середньому. Розкид великий в ОБОХ
випадках — це чесно відображає реальну архітектуру: відновлення вмикається
лише коли власний стан мухи переходить у `idle`/`groom`/`atfood` (НЕ під час
звичайної ходьби), а це рішення періодичне (раз на ~1.6-2.6с, залежно від
`DRIVE_CURIOSITY`) — тому короткі паузи інколи "встигають" захопити вікно
відпочинку, інколи ні. Прискорення робить КОЖНЕ таке вікно ефективнішим,
але не гарантує, що воно трапиться в кожній конкретній паузі.

Побічно під час інтенсивного синтетичного тестування (40 форсованих зльотів
поспіль з миттєвим скиданням стану) двічі впіймано незалежну, вже наявну
раніше помилку (`Cannot read properties of undefined (reading 'x0')` —
десь у виборі кадру спрайту при нетиповій послідовності станів). Не пов'язана
з цими змінами й не відтворена при звичайній грі — залишаю як факт на
майбутнє, не виправляю зараз (поза межами цього запиту).

### Баг: "не їсть, коли голодна" — справжня причина й виправлення

Знайдено живою симуляцією (муха йде до їжі, hunger=90%, 100px дистанція):
приблизно на 10-й секунді підходу муха РАПТОМ злітала й відлітала далеко,
лишаючи їжу поза радіусом нюху — голод стояв на 100% нескінченно, бо
"рішення поїсти" (`state==='atfood'`) просто ніколи не наставало знову.

Інструментовано `takeoff()` напряму — злітала з `reason:'escape'`,
**`fear: 0`**, `flight: 16` (переріз порогу), `visCloseStreak: 4`. Тобто це
був НЕ справжній переляк, а зоровий `VIS_LC` (зіткнення/стіна поруч — див.
розділ "Зір" вище), який просто накопичив достатньо `accumFlight`, поки муха
йшла повз меблі до їжі. `escapeUrge` перевіряв лише `brain.flight > поріг`,
без жодного розрізнення "справжня загроза" проти "фонового зорового шуму".

Виправлено точково: `escapeUrge` тепер додатково враховує `BRAIN.drives.fear`
— якщо переляку НЕМАЄ (`fear ≤ 0.02`, суто зоровий сигнал) І муха ЗАРАЗ
активно переслідує конкретну їжу (`chaseFood`) — зліт придушується, їжа
виграє. Справжній переляк (клік/струс, `fear>0.02`) і далі скасовує їжу
БЕЗУМОВНО, як і мало бути. Перевірено: 7/7 свіжих прогонів тепер успішно
доїдають (раніше — регулярно НЕ доїдали, зависаючи на 100% голоду); окремо
підтверджено, що справжній клік-переляк (`fear=0.15`) і далі коректно
перериває переслідування їжі.

### Ще правки за відгуком (баланс)

- **"Витривалість польоту" → "Витривалість"** (UI-лейбл, обидва місця).
- **Втрата витривалості в польоті уполовинена**: `FATIGUE_GAIN_AIR_LIT/DARK`
  0.02/0.04 → **0.01/0.02**.
- **Вплив ходьби зменшено ще вдвічі** поверх попереднього:
  `FATIGUE_GAIN_WALK_LIT/DARK` 0.003/0.006 → **0.0015/0.003**. Асиметрія
  політ/ходьба (~6.7×) лишається тією самою.
- **Настання голоду уполовинене**: `HUNGER_RATE_DIGEST` 0.8→**0.4**,
  `HUNGER_RATE_HUNGRY` 100/40→**100/80**.
- **Швидкість польоту +10% поверх попередньої бази**: `FLIGHT_SPEED_MULT`
  1.1→**1.21** (сукупно +21% від початкової до минулого запиту).
- **Стрес від голоду до 100%**: вже реалізований минулого разу механізм
  (постійний тиск, поки голод>80%) підтверджено живим тестом — при сталих
  100% голоду стрес плавно доходить до 100% і тримається там (~20с до стелі,
  див. графік у коді).

### Гравітація: справжній баг, не лише відчуття

За скаргою "не літає високо, короткі дистанції, дуже втомлюється" —
`LIFT_FORCE_MAX = GRAVITY_ACCEL * 0.9`. Тобто НАВІТЬ на нульовій втомі
підйомна сила компенсувала лише 90% гравітації — муха структурно ЗАВЖДИ
тонула під час польоту, незалежно від того, наскільки вона свіжа. Це не
відчуття, а реальний баг: перевірено прямим розрахунком (`netAccel` на
fatigue=0 був позитивний, тобто вниз) і живою симуляцією (навіть із сильним
стартовим підйомом вгору ерозія накопичувалась щокадру без спаду).

Виправлено: `LIFT_FORCE_MAX = GRAVITY_ACCEL` (100%, не 90%) — на нульовій
втомі політ тепер справді рівний/висхідний, а не завжди спадний. Зі
зростанням втоми підйомна сила природно падає нижче гравітації, і політ
починає просідати — це й мало бути єдиним джерелом зниження висоти, не
структурна межа "навіть по-свіжому".

Додатково — за повторним відгуком "ще забирає багато витривалості":
`FATIGUE_GAIN_AIR_LIT/DARK` уполовинені ЗНОВУ (0.01/0.02 → **0.005/0.01**).
І `minAirTicks` для дослідницьких перельотів (не через переляк) збільшено з
10 до **20 тактів** (5с→10с) — раніше приземлялась одразу по спливанню
мінімуму, бо без реального переляку "заспокоєння" виконується майже
миттєво; тепер є більше часу реально скористатись виправленою гравітацією.

**Виміряно (свіжі прогони, автономний зліт без кліку/струсу):**

| | До | Після |
|---|---|---|
| Дослідницький політ: тривалість | ~4.6с | ~9.7с |
| Дослідницький політ: дистанція | ~217px | 224-316px |
| Дослідницький політ: набір висоти | завжди 0 (тонула) | до 308px (залежить від напрямку) |
| Втома після польоту | 0.06 | 0.07-0.2 |

Клік-втеча (`~3с`, `peakFear`-калібрування з ранньої сесії) НЕ зачеплена —
той механізм навмисне короткий і лишається таким.

Регресія 300с: 0 помилок, 0 виходів за межі canvas, escape/дослідницьких
зльотів у здоровому діапазоні (9/12).

### Нова мапа зон, фон, ніч, вертикальні поверхні

За зображеннями користувача (`~/Desktop/Fly/`: `background_day.jpg`,
`background_night.jpg`, `background_mapping.jpg`, `light source.jpg`) —
нова растрова карта зон замість старої. Побудована програмно (Python/PIL,
HSV-класифікація кольорів з референсу, не вручну), перевірена візуальним
накладанням перед вбудовуванням — див. методологію нижче.

**Нові/змінені зони (`zoneAt()`):**
- `walk` (зелений) — стіл, як і раніше.
- `flight` (синій) — верх системного блоку, верх монітора, підвіконня.
  Досяжно лише польотом, як і раніше.
- `pcside` (пурпуровий, НОВИЙ) — бічні панелі системного блоку й монітора.
  За прямою вказівкою: ПОКИ лише пролітати, НЕ приземлятись (виключено зі
  списків придатних для посадки зон). Уточнення й кадри — пізніше.
- `floor` (жовтий) — тепер визначається кольором растру, а НЕ прямокутником
  за Y-координатою (`FLOOR_Y_START` видалено повністю) — точніше під реальну
  діагональну межу підлоги на новій артворці.
- `hot` (червоний, гаряча чашка) — збережено. Мокап користувача НЕ позначав
  чашку окремим кольором (вона просто лежить на зеленому столі), тож
  позицію інферив сам за розташуванням чашки на `background_day.jpg` (маленька
  пляма ~10px радіусом на місці кави). Якщо це небажано — скажи, приберу.
- `vertical` (НОВИЙ, усе інше — стіна, рама вікна, скло, передня панель
  системного блоку) — за прямою вказівкою: муха МОЖЕ там ходити й
  приземлятись. Анімація лазіння — ТИМЧАСОВА ЗАГЛУШКА: поки що рендериться
  звичайною анімацією ходьби (кадрів лазіння ще нема, обіцяні пізніше) —
  коли прийдуть, підключаться так само, як діагональні кадри ходьби.

**Зона екрана монітора** (ціан на референсі) — зафіксована координатами
(`MONITOR_SCREEN_ZONE`), нічого не виводить, лише "закладка" на майбутнє,
як і просив користувач.

**Світло:** позиції монітора/вікна оновлені під нову композицію. За прямою
вказівкою — вікно менш привабливе (`strength` 0.75→0.5), монітор — більший
радіус і сильніша привабливість (`radius` 110→170, `strength` 0.4→0.6).

**Ніч:** нова кнопка "Ніч"/"День" перемикає `isNight`, рендер вибирає
`deskNight` замість `desk`. Фонові зображення — JPEG (не PNG, як `zoneMap`) —
фотографічний контент стискається набагато краще, ~70KB/54KB замість ~400KB
кожне при PNG.

**Методологія побудови растру** (для майбутніх подібних завдань): пряма
HSV-класифікація кольорів мокапу спершу хибно ловила листя дерева за вікном
як "зелений стіл" (той самий відтінок hue) — виправлено звуженням
hue-діапазону з підтвердженням на реальних semplах пікселів (не на око).
Ділянку екрана монітора довелось явно виключити з класифікації — іконка
Figma на скріні (червоний/фіолетовий/синій/зелений) давала хибні одно-
пікcельні спрацювання. Перед вбудовуванням растр накладався напівпрозоро на
реальний фон і звірявся візуально — збіг вийшов дуже точним, попри те що
референс-зображення (572×1024) і фонові артворки (577×1024) — окремі
генерації з невеликою різницею пропорцій.

Перевірено живою регресією (2×300с, свіже завантаження): 0 помилок, 0
виходів за межі, `zoneTimeCounts` підтверджує, що муха реально відвідує й
приземляється на нову `vertical` зону (не лише walk/floor), термальна зона
чашки (`HEAT_SOURCE`) знаходиться коректно.

### Баг: муха наполовину вилазила за край фону

Причина: `clampCanvasX`/`clampCanvasY` тримали `pos` у буквальних межах canvas
(`[0,379]`/`[0,679]`) БЕЗ запасу під власну ширину спрайту — а спрайт
малюється ЦЕНТРОВАНИМ на `pos`. Раніше це рідко було помітно, бо непофарбовані
краї були зоною `'obstacle'` (недосяжною) — після сьогоднішньої зміни на
`'vertical'` (ходима) муха реально доходить до самого краю стіни, і половина
найширшого кадру (до ~51px, виміряно по `walkSheet`/`walkDiagFrames`/
`groomFramesA`) рендерилась за межами canvas. Виправлено: новий
`GROUND_MARGIN=26` (половина найширшого кадру із запасом) у обох клемп-
функціях. "Біла вертикальна смуга" з опису — не відтворилась у прямому
рендер-тесті; можливо той самий корінь або тимчасовий артефакт скріншота,
не чіпав — скажи, якщо лишиться.

Перевірено: живий рендер на самому лівому краю (GROUND_MARGIN) — спрайт
повністю в кадрі; регресія 300с — 0 помилок, `maxGroundX` рівно
`379-GROUND_MARGIN`, як і очікувалось.

### Біла смуга — справжня причина знайдена й виправлена

Не відтворювалась у попередній перевірці, бо я дивився не в те місце.
Користувач надіслав приклад — перевірив піксельно (Python/PIL) і знайшов:
у `background_day.jpg` і `background_night.jpg` **найправіший стовпчик
пікселів (x=576) чисто білий (255,255,255) по всій висоті в ОБОХ файлах** —
артефакт самого згенерованого зображення (не рендерингу гри). При
масштабуванні 577→380 він розмазувався в останні пікселі. Виправлено
обрізанням 3px справа перед масштабуванням (не "збільшенням фону", як
пропонував користувач, — обрізання дефектного краю точніше і не тягне
зайвого масштабування решти сцени). Перевірено пікселями після фіксу — 0
білих пікселів на краю.

### Лазіння по вертикальних поверхнях — підключено

5 кадрів користувача (`fly_climbing.png`) нарізані по альфа-межах кожного
кадру (Python/PIL, програмно, той самий підхід, що й для інших аркушів),
вирівняні на спільний рядок-якір (щоб тіло не "смикалось" між кадрами),
запаковані в один PNG (палітра 64 кольори — 333KB→45KB, без помітної втрати
якості) і вбудовані як новий `climbImg`.

**Орієнтація — суцільний поворот (`ctx.rotate`), а не дзеркало.** Базова
поза — "лізе вгору". Кут повороту рахується напряму з `Math.atan2(vy,vx)`,
тож голова завжди веде в напрямку руху — природно і для руху вниз теж
(муха повертається "догори лапками" відносно екрана, що коректно: комаха,
що повзе вниз, веде головою вниз). Свідомо НЕ додав окреме дзеркало поверх
повороту: `atan2` вже коректно охоплює будь-який вектор, включно з
лівим/правим — окреме дзеркало на цьому ж спрайті дублювало б і ЛАМАЛО б
орієнтацію (подвійне віддзеркалення). Перевірено вручну для 3 напрямків
(вгору/вправо/вниз) — коректно в усіх.

Перевірено живою регресією 300с: 0 помилок, `zoneTimeCounts` показує, що
`vertical`-зона реально й активно використовується (13150 тактів із ~18750)
— тепер це не рідкісний випадок, а звичайна частина гри.

### Динамічна швидкість ходьби (замість єдиного темпу)

За описом користувача (звірено з реальною біомеханікою *Drosophila* —
спокійний пошук ≈2-5мм/с хаотично з частими зупинками, втеча/загроза —
різко швидше ≈20-30мм/с+, частота кроків росте зі швидкістю): погодився,
реалізував. Гра стилізована й не калібрована в мм (як і вся фізика тут) —
тому масштабується та сама ПРОПОРЦІЯ відносно старої фіксованої бази (0.3),
а не вигадане мм-переведення.

- `groundSpeed()` — нова базова швидкість: `WALK_SPEED_CALM_MIN..MAX`
  (0.18-0.35, легкий розкид замість фіксованого 0.3) × множник від
  `brain.fear` (до ×8 на повному страху). Керує ТИМ САМИМ `brain.fear`, що
  й поріг зльоту-втечі — ходьба прискорюється ГРАДУЙОВАНО ще ДО того, як
  накопичений `accumFlight` перетне поріг реального польоту, а не
  перемикається різко "спокійно/панічно".
- `walkDir()` — обгортка над `brainDir()` новою швидкістю; коли справді
  спокійна (`fear<0.05`) — 15% шанс замість руху обрати паузу (`vx=vy=0`,
  "часті зупинки"). Під час страху НЕ зупиняється.
- Застосовано в усіх точках вибору курсу ходьби: після зльоту/посадки,
  групмінгу, відпочинку, їжі, звичайного передумування курсу, і навіть у
  прицільному русі до їжі/світла (масштабується тим самим `groundSpeed()`,
  без випадкового напрямку — там пауза недоречна).
- Частота зміни кадру ходьби тепер масштабується з РЕАЛЬНОЮ швидкістю
  (реальна залежність у біомеханіці ходи комах) — 110мс лишається еталоном
  саме на старій калмbase-швидкості (0.35), спринт скорочує інтервал до
  30мс (клемп).
- Кути повороту (30-90°) НЕ чіпав — уже наявний `brainDir()` (curiosity-
  залежний `spread`/`turnMagnitude`) дає приблизно такий діапазон, і це
  ретельно відкалібрована минулого разу система (виправлення систематичного
  зсуву в один бік) — зайвий ризик без явної потреби.

Перевірено: `groundSpeed()` при fear=0 дає 0.18-0.35, при fear=1 — 8.07×
швидше (1.48-2.70); паузи — 22/200 при спокої, 0/200 під час страху;
інтервал кадру — 128мс на спокійній швидкості (≈стара 110мс), 30мс на
спринті. Живий сценарій (м'який переляк, НЕ до порогу польоту): швидкість
реально зросла на наступному рішенні курсу (0.34→0.55). Регресія 300с — 0
помилок, 0 виходів за межі.

### Швидкість польоту +20%

`FLIGHT_SPEED_MULT`: 2.42 → **2.904**. Параметри витривалості
(`FATIGUE_GAIN_AIR_*`, `FATIGUE_RECOVERY_RATE_BASE`) не чіпав, за прямою
вказівкою.

### 'pcside' (бокова стінка ПК/монітора) тепер теж лазима

Раніше — лише "пролітати". Тепер: `attemptLand()`/`findNearestLandable()`
додано до списків придатних для посадки, `altZoneSeen` (зір) теж рахує її
альтернативою.

**Анімація — за буквальною вказівкою, не custom-кадри.** Використано
`walk_diag1` (`walkDiagFrames`, той самий аркуш, що й `diagMode===1`), а не
`fly_climbing.png`: `ctx.scale(-1, mirrorY); ctx.rotate(-45°);` — mirror
horizontal завжди, mirror vertical додатково лише коли останній рух був
униз (`lastPcsideDown`, персистентна змінна — читається й під час
стоп-кадру, коли `vy≈0`).

**Порядок трансформів — контрінтуїтивний, перевірено рендер-тестом.** Canvas
застосовує ОСТАННІЙ виклик до сирого спрайту ПЕРШИМ — тобто щоб отримати
видимий ефект "спершу поворот, тоді дзеркало" (як просив користувач), у коді
виклики йдуть У ЗВОРОТНОМУ порядку (`scale` перед `rotate`). Перевірено
візуально обидва порядки поруч: "неправильний" (виклики
rotate-then-scale) давав сплюснуту, радше горизонтальну позу; обраний —
чітко вертикальну "лізе вгору"/"лізе вниз" (голова вгорі/внизу відповідно).

**Стоп-кадр на vertical/pcside** (за прямою вказівкою — зупинка й
відпочинок мають бути стоп-кадром анімації лазіння, не окремою
"ідле"-анімацією): при вході в `idle` на цих зонах `frameIndex` НЕ
скидається на 0 (як для звичайного ідле); під час `idle` він НЕ
циклюється (пропускаємо крок анімації); рендер для `idle` на цих зонах
перевикористовує ту саму гілку кадру/повороту, що й ходьба, просто
`vx/vy≈0` — тому кут/дзеркало читають останнє ЗБЕРЕЖЕНЕ значення
(`lastClimbRotation`/`lastPcsideDown`), а не скидаються на базову позу.

**Швидкість лазіння ×2** (за прямою вказівкою) — новий `CLIMB_SPEED_MULT=2`
множник усередині `groundSpeed()`, застосовується автоматично й на
`vertical`, і на `pcside` (та сама функція, що й для fear-масштабування
минулого разу — множники комбінуються).

Перевірено: рендер-тест підтвердив коректні пози вгору/вниз; стоп-кадр
підтверджено (з правильним `stateUntil` — перший тест мав хибний стан,
frameIndex дійсно застигає); швидкість ×~1.8-1.9 від звичайної в
10-прогонному семплі (очікувані ~2× з поправкою на випадковий розкид).
Регресія 300с (2 прогони) — 0 помилок, 0 виходів за межі; `pcside` — мала
зона, природно не відвідана в цих конкретних 300с прогонах (перевірено
прямим позиціюванням натомість).

### Зона чашки ('hot') — уточнена форма під реальний спрайт

Була груба заглушка: суцільне червоне коло ~21×21px, приблизно
по центру чашки, що не покривало ні ручку, ні більшу частину корпусу.

Замінено на форму, що щільно повторює контур намальованої чашки
(звірено піксель-у-піксель з `desk.jpeg` через зумований grid-оверлей):
заокруглений прямокутник для корпуса (`[325,351]–[360,394]`, radius 8) +
еліпс для ручки (`[356,364]–[375,390]`) з вирізаним отвором ручки назад
у зелений (`walk`), щоб кільце ручки читалось коректно. Стара пляма
стерта назад у зелений (`walk`) перед малюванням нової форми.

Це вже не просто "приземлятись не можна" — центроїд для теплового поля
(`computeHeatSource()` → `HEAT_SOURCE.{x,y}`) тепер зміщується разом із
фактичним центром чашки (~346,373 замість ~340,358), тож і "нюхове"
відчуття тепла (нижче) стало точнішим.

Перевірено: `zoneAt(346,373)` → `'hot'`; офлайн-накладання нової
zone-маски на `desk.jpeg` (6× zoom) показує щільне прилягання до контуру
чашки й ручки; живий рендер у браузері — без консольних помилок.

### Чи відчуває муха температуру? Так — це вже реалізовано, не нова фіча

У коннектомі вже є повноцінна термосенсорна пара нейронів
`THERMO_WARM`/`THERMO_COOL` (взаємне гальмування), і вона підключена
до РЕАЛЬНОГО теплового поля навколо зони `'hot'`, а не просто до факту
дотику:

- `computeHeatSource()` (рахується одноразово при завантаженні мапи
  зон) знаходить центроїд усіх `'hot'`-пікселів і задає йому
  `radius: 130` — це "радіус відчутності" тепла, свідомо набагато
  більший за сам фізичний розмір чашки, бо справжні мухи відчувають
  випромінюване тепло антенами здалеку, не лише дотиком.
- `findHeatLevel(x,y)` — лінійне згасання `1 - d/radius` від центру
  до краю радіуса.
- `BRAIN.stimulate.temperature = 0.5 + findHeatLevel(pos.x,pos.y)*0.5`
  щотика — тобто 0.5 = нейтральна "кімнатна" температура, до 1.0 біля
  самої чашки.
- Коннектом: `temperature > 0.65` → збуджує `THERMO_WARM` →
  через `LH_AV`/`DRIVE_FEAR` штовхає муху уникати зони ще ДО фізичного
  зіткнення з межею `'hot'` (це і є коментар у коді "ще ДО того, як
  вона впреться в межу").

Тобто термо-уникнення вже було і вже жило — форма зони була лише про
фізичну межу приземлення. (Далі, в наступному запиті, `THERMO_COOL`
із "мертвої гілки за браком стимулу" перетворився на реально
використовувану — див. розділ нижче.)

### Повна термальна карта сцени — кожен об'єкт своя температура, день/ніч

За прямою вказівкою користувача: ПК/системний блок трохи теплі (однаково
вдень і вночі), стіл нейтральний, підлога прохолодна вдень і холодніша
вночі, вікно тепле вдень і холодне вночі, чашка гаряча вдень і холодна
вночі. Один-єдиний `HEAT_SOURCE` (чашка) замінено на повну систему:

**Чому НЕ можна було просто взяти растрові зони `'flight'`/`'pcside'`
цілком.** Обидва кольори на мапі зон покривають по КІЛЬКА різних
фізичних об'єктів одразу: `'flight'` (синій) — і верх системного блоку,
і верх монітора, і підвіконня; `'pcside'` (пурпуровий) — і бік корпуса,
і бік монітора. Якби рахувати єдиний центроїд одного кольору (як
`computeHeatSource()` робить для суцільної зони `'hot'`), він опинився
б десь ПОСЕРЕДИНІ між об'єктами й не відповідав жодному з них. Замість
цього для `'flight'`/`'pcside'` написано ad-hoc connected-component
пошук (BFS-заливка по 380×680 растру офлайн, у Python — 5 окремих
зв'язних плям знайдено автоматично: верх СБ, верх монітора, підвіконня,
бік СБ, бік монітора), і саме їхні виміряні центроїди/bbox стали
координатами нових точкових джерел `THERMAL_SOURCES`.

**Структура:**
- `THERMAL_SOURCES` — масив `{name,x,y,radius,day,night}`, за тим самим
  патерном, що й `LIGHT_SOURCES` (точка + радіус згасання): 5 записів
  (верх/бік СБ, верх/бік монітора, вікно+підвіконня разом — одне джерело
  на весь простір навколо вікна, від рами до підвіконня).
- Підлога — НЕ точкове джерело (форма зони `'floor'` велика й нерівна, а
  не пляма), а рівномірний офсет `FLOOR_TEMP_OFFSET.{day,night}`, що
  застосовується скрізь, де `zoneAt(x,y)==='floor'`.
- Чашка (`HEAT_SOURCE`) лишилась як була — обчислюється від фактичних
  пікселів зони `'hot'` (не задана вручну, бо форма складна:
  корпус+ручка), просто додано `day`/`night` знак/силу до тієї ж
  структури.
- `computeTemperatureOffset(px,py)` — все з попереднього пункту разом,
  бере НАЙСИЛЬНІШЕ (за модулем) джерело в радіусі, а не суму/середнє
  (той самий принцип, що й `findLightPull` для світла — інакше на межі
  двох джерел вийшло б щось середнє, що не відповідає жодному).
- `BRAIN.stimulate.temperature = clamp(0.5 + computeTemperatureOffset(...), 0, 1)`
  замінив стару формулу з `findHeatLevel` (видалено, була єдиним
  джерелом).

**Підібрані значення (offset від нейтральних 0.5; поріг THERMO_WARM
>0.65, THERMO_COOL <0.35):**

| Об'єкт | День | Ніч | Коментар |
|---|---|---|---|
| Верх СБ / верх монітора | +0.12 (temp 0.62) | +0.12 (temp 0.62) | "трохи теплі" — свідомо ПІД порогом 0.65, електроніка гріє стабільно, не як загроза |
| Бік СБ / бік монітора | +0.10 (temp 0.60) | +0.10 (temp 0.60) | те саме, трохи слабше (вужча панель) |
| Стіл | 0 (temp 0.50) | 0 (temp 0.50) | немає джерела — нейтральна база сама по собі |
| Вікно/підвіконня | +0.18 (temp 0.68) | −0.22 (temp 0.28) | вдень трохи ПЕРЕВИЩУЄ поріг у самому центрі (сонце реально гріє помітніше за ПК) — єдине денне джерело, крім чашки, що дає реакцію; вночі чітко нижче порогу COOL |
| Підлога | −0.10 (temp 0.40) | −0.18 (temp 0.32) | вдень під порогом (просто прохолодніше повітря), вночі трохи нижче порогу — легке уникання |
| Чашка | +0.45 (temp 0.95) | −0.30 (temp 0.20) | найсильніше джерело в обидва боки — гаряча кава вдень, вихолола/холодна вночі |

Тепер `THERMO_COOL`, який раніше був "мертвою гілкою за браком стимулу"
(див. попередній розділ), реально спрацьовує вночі біля вікна, підлоги
й чашки.

Перевірено: живий рендер у браузері, прямі виклики
`computeTemperatureOffset()`/`zoneAt()` у всіх 6 точках (центри кожного
джерела) в обох режимах `isNight` — всі значення збігаються з таблицею
вище; консоль — без помилок за час нормальної роботи.

### 'pcside' "не лазима" — знайдено СПРАВЖНІЙ баг, не лише обмеження дизайну

Скарга користувача: "відчуття наче вона не може лазити там чи
приземлятись туди, чи перелазити вільно з будь-якої доступної суміжної
зони". Протестовано форсованою посадкою й прямими викликами
`tryMoveGround()`/`loop()` у детермінованому режимі (заморожений
`requestAnimationFrame`, ручні тактові виклики) — знайдено ДВІ окремі,
незалежні причини.

**Причина 1 (дизайн, за попередньою вказівкою користувача): переходи між
зонами вимагали польоту.** `blocked(x,y)` раніше дозволяв рух лише в межах
ТІЄЇ САМОЇ `surfaceZone` — тобто щоб перейти з `'walk'` (стіл) на
`'pcside'` (бічна панель ПК/монітора), навіть коли вони пиксельно межують
(муха просто впритул до стінки), треба було злітати й приземлятись
заново. Реалістично це вірно лише для СПРАВДІ роз'єднаних поверхонь
(верх монітора — `'flight'`, куди справді треба залітати), але НЕ для
поверхонь, що фізично торкаються одна одної (стіл → бічна стінка
корпуса — це один безперервний контакт лапок, польот тут не потрібен).
**Виправлено:** новий `GROUND_CRAWL_ZONES = {'walk','floor','vertical',
'pcside'}` — вільне повзання між БУДЬ-якими двома зонами з цього набору,
`surfaceZone` синхронізується на кожному кроці (`syncSurfaceZone()`), щоб
анімація/швидкість одразу відповідали новій поверхні. `'flight'`
(верх ПК/монітора/підвіконня) свідомо ЛИШИВСЯ "лише польотом" — за
ПЕРШОЮ вказівкою користувача на початку цієї гілки роботи, навіть
попри пиксельне сусідство з `'pcside'` на растрі.

**Причина 2 (справжній баг у `tryMoveGround()`, не дизайн):** усі три
"часткові" гілки руху (повний рух, лише-x, лише-y) рахували себе
успішними за критерієм `!blocked(target)`, НЕ перевіряючи, що `target`
справді ВІДРІЗНЯЄТЬСЯ від поточної позиції. Коли обчислена ціль
збігається з поточною (типово: рух суто вертикальний і заблокований по
y — тоді "рух по x" тривіально дорівнює поточному x; або впритул до
краю канвасу `GROUND_MARGIN`, де рух ще й далі штовхає ЗА межу) — "рух
на 0px від уже легальної позиції" за визначенням НІКОЛИ не заблокований,
і гілка мовчки "перемагала" щотакту, не рухаючи муху НІ НА ПІКСЕЛЬ і не
доходячи до реального fallback (посунутись по іншій осі чи випадковий
обхід). Візуально — саме "тупцяє на місці біля стінки, не може залізти".
Підтверджено регресією ДО фіксу: до 265 тактів (~8.7с) поспіль без руху.
**Виправлено:** кожна гілка тепер вимагає ФАКТИЧНОГО зрушення позиції
(`nx!==pos.x || ny!==pos.y` і т.д.), інакше падає до наступної,
включно з випадковим обходом.

Перевірено: після обох фіксів — `maxStuckStreak:0, stuckEvents:0` на
тому самому сценарії, де раніше було 265; природний (не форсований)
15-хвилинний прогін тепер реально відвідує `'pcside'` (раніше — 0 за
900с); скріншот підтверджує коректний рендер пози лазіння під час
живого переповзання зі столу на бічну панель; 20-хвилинна комплексна
регресія (з кліками/струсами/скиданням їжі/перемиканням дня-ночі) —
0 помилок, 0 NaN, 0 виходів за межі.

### Аудит "чи скрізь рішення справді мухи, а не рандому"

Переглянуто кожне використання `Math.random()` у файлі. Висновок:
дисципліна вже висока, нових "фальшивих рішень" не знайдено — випадковість
лишається виключно там, де це або (а) біологічний шум ПОВЕРХ параметра,
який задає мозок (розкид темпу ходьби в межах `WALK_SPEED_CALM_MIN/MAX`,
напрямок повороту в `brainDir()` — межі й ймовірність повороту рахує
CX_HDELTA/DRIVE_CURIOSITY, сам випадковий біт — лише "вліво чи вправо в
межах цього"), (б) зовнішній стимул середовища, не рішення мухи (осідання
пилу, тип/позиція тестової їжі при натисканні кнопки), або (в) чисто
фізичний рефлекс-заглушка (аварійний обхід перешкоди в
`tryMoveGround()`, коли всі "розумні" варіанти вичерпано). Жодних змін
не знадобилось.

**Додано для реалістичності — вікно/монітор тепер по-різному притягують
вдень і вночі.** До цього `isNight` був суто косметичним (лише фон +
щойно додана температура) і ніяк не впливав на те, КУДИ муха реально
летить, — попри повноцінну симуляцію дня/ночі. За тим самим патерном, що
й `THERMAL_SOURCES`: `LIGHT_SOURCES` тепер має `day`/`night` силу
притягання замість єдиної `strength` (`LS(L)` — helper, що вибирає
потрібну). Вікно вночі — темна шибка без сонця, майже не притягує
(0.5→0.15); екран монітора вночі, навпаки, виглядає ЯСКРАВІШИМ на
контрасті з темною кімнатою — реальний ефект, через який комахи вночі
летять на світлові прилади, а не у вікна (0.6→0.75). Перевірено:
денна 15-хвилинна регресія — 570 з 900с на `'flight'` (переважно
підвіконня); та сама регресія вночі — 0с на `'flight'`, натомість
активність зміщується до монітора; 0 помилок.

**Ідеї на майбутнє (НЕ реалізовано цього разу — для окремого запиту):**
- Реальне поїдання їжі (нижче) — найбільш відчутний наступний крок.
- Напрямок втечі (`takeoff()` після кліку/струсу) зараз НЕ враховує, ЗВІДКИ
  прийшов подразник (позицію кліку) — це генерична CX_HDELTA-ціль, не
  буквальна втеча "геть від точки загрози". Реальний ескейп-рефлекс мухи
  напрямлений.
- Амбієнтний протяг/вітер від вікна (зараз `BRAIN.stimulate.wind`
  тригериться лише зі струсом, хоча відкрите вікно реалістично теж мало
  б додавати слабкий фоновий MECH_JO).

### Статус-рядок тепер показує нейрони конектома, що спрацювали, а не текст дії

За прямою вказівкою користувача: замість заготовленого опису ("муха
гуляє", "муха летить" тощо) рядок під канвасом показує буквально, які
нейрони РЕАЛЬНО перетнули поріг спрацювання (`fireNeuron`) цього такту —
в один рядок, через кому.

- `BRAIN.runconnectome()` тепер накопичує `BRAIN.firedThisTick` — список
  імен немоторних нейронів, що спрацювали САМЕ цього виклику (не всі з
  ненульовим потенціалом, а конкретно ті, що перетнули `fireThreshold` і
  скинулись).
- `rebuildNeuronPages()` (UI-шар, окремо від мозку) раз на такт (500мс,
  всередині `runBrainTick`) перетворює цей список на "сторінки" — кожна
  вимірюється через offscreen-canvas `measureText` (той самий шрифт, що
  й CSS `#status`) і гарантовано влазить в один рядок шириною `#status`.
  Якщо повний список не влазить — ділиться на кілька сторінок.
  `#status` отримав `white-space:nowrap; overflow:hidden` (раніше міг
  переноситись на 2 рядки).
- `loop()` щокадру гортає сторінки по черзі (`NEURON_PAGE_MS=2200`), якщо
  їх більше однієї.
- Миттєві реакції (клік/струс/поїдання), які й раніше пропускали такт
  конектома напряму (`BRAIN.runconnectome()`/`BRAIN.update()` поза
  звичайним 500мс тактом — щоб реакція була видима негайно), тепер
  ТАКОЖ одразу викликають `rebuildNeuronPages()` в тому ж місці —
  інакше рядок показував би застарілий список аж до наступного
  планового такту.
- `hotMsg`/`cantLand` (попередження "тут не можна сісти" тощо) свідомо
  ЛИШИЛИСЬ звичайним текстом, не нейронами — це не опис автономної дії
  мухи, а зворотний зв'язок для КОРИСТУВАЧА про результат його власної
  спроби посадити муху кліком, тому пріоритет над тікером збережено.

Перевірено: живі виклики показують реальні імена (`CX_EPG, CX_PFN,
CX_FC`, `DN_STARTLE, CX_PFN` після струсу й т.д.); штучний список з 18
імен коректно розбився на 5 сторінок, кожна ≤376px; `scrollWidth ===
clientWidth` і `scrollHeight === clientHeight` для найдовшої сторінки
(підтверджено — рядок дійсно один, без переносу); гортання сторінок
підтверджено вручну за 3 такти.

### Тарган — проста сутність без мозку/AI

За буквальною специфікацією користувача: чорний прямокутник 14×6px,
чисто випадкове блукання (fast 0.7 / slow 0.25 / still, зміна стану
кожні 0.7–2.5с, випадковий кут), з'являється ОДИН раз — через 10с після
ПЕРШОГО "Кинути їжу", на тій самій зоні (`'walk'` чи `'floor'`), куди
впала ЦЯ перша їжа, прив'язаний до цієї зони назавжди, повторні кліки
"Кинути їжу" нічого не роблять, поки він живий; "Очистити" прибирає
його (і скасовує заплановану появу, якщо вона ще не настала) — наступна
їжа знову планує нову появу. Жодного зв'язку з `BRAIN`/flybrain —
окремий незалежний такт `updateRoach(t)`, викликається з `loop()` поруч
із рештою сутностей сцени (їжа/какульки), але не всередині гілки
стану/мозку мухи.

**Параметри користувача лишились без змін** — вони вже добре
відповідають реальній тарганячій ходьбі (різкі спринти, повільне
дослідження, завмирання), редагувати не було сенсу.

**Один технічний нюанс, доданий від себе:** таймер появи —
`performance.now()`-мітка (як `hotMsgUntil`/`shakeUntil` та інші вже
наявні таймери в файлі), а НЕ `setTimeout` — це узгоджується з рештою
кодової бази й лишається детерміновано тестованим тим самим методом
"заморозь rAF, жени `loop(t)` вручну", що й усюди в цій сесії.

**Межа зони — та сама СТРУКТУРНА пастка, що вже виправлялась для
мухи в `tryMoveGround()` (див. вище), тож одразу написана правильно**:
рух таргана до межі своєї зони не "зависає" на нулі — при блокуванні він
одразу пробує 8 випадкових напрямків (як фолбек мухи), і рахує спробу
успішною лише коли позиція СПРАВДІ зрушила.

Перевірено: перший `dropFood()` планує появу на зоні/позиції ЦІЄЇ їжі;
повторні `dropFood()` до появи НЕ змінюють заплановану мітку; після
появи `roach.zone` збігається з зоною першої їжі; 300с прямого прогону
`updateRoach()` — 0 виходів за межі власної зони; окремі виміри показали
фактичну тривалість стану точно в межах 700–2500мс (мінімум 825,
максимум 2361 на вибірці); "Очистити" коректно обнуляє й таргана, і
заплановану появу; рендер підтверджено читанням `getImageData` навколо
позиції таргана (71 темний піксель з 200 у вікні 20×10 — узгоджується з
поверненим на 14×6 прямокутником); комплексна 400с регресія з мухою й
тарганом одночасно — 0 помилок.

### Тарган → зір/дотик/втеча мухи — тепер реально підключено

Перша вказівка користувача сказала "поки що окремо, без мозку"; друга,
уточнююча — "зроби так, щоб муха реагувала на таргана і бачила його
очима, і це тригерило її нейрони. Коли тарган фізично чіпляє муху, або
надто близько — це теж має їй передатись на вибір: або швидко відповзти,
або злетіти і тікати." Реалізовано БЕЗ жодного нового "тарганячого"
нейрона — лише через нейрони, що вже існували в конектомі:

- **Бачить (здалеку, `ROACH_SEE_DIST=120px`), лише коли тарган
  РУХАЄТЬСЯ** (`'fast'`/`'slow'`, НЕ `'still'`) — `VIS_LO` (той самий
  канал, що й для країв стін). Нерухомий тарган майже непомітний — реальні
  комахи значно чутливіші до руху, ніж до статичної форми.
- **Надто близько** (`ROACH_CLOSE_DIST=55px`) — `VIS_LC` (loom), з тим
  самим дебаунсом (`VISION_LOOM_STREAK_NEEDED`, ~1.5с поспіль), що й для
  стін — один випадковий кадр близькості не лякає миттєво.
- **Фізичний дотик** (`ROACH_TOUCH_DIST=26px`) — той самий шлях, що й
  клік по мусі (`VIS_LC`+`DN_STARTLE` напряму), ПЛЮС уперше реально
  задіяний `BRAIN.stimulate.touch`/`touchLocation` — він існував у
  вихідному flybrain із самого початку (`d.fear+=0.3`, `d.groom+=0.2`,
  `MECH_BRISTLE`), але досі в цій грі НІЧОГО його не тригерило (перевірено
  грепом — жодного `.touch = true` в усьому файлі). Дотик таргана став
  першим реальним джерелом для цього шляху.

**Справжній баг, знайдений тестуванням: `dendriteAccumulateScaled`
округлює через `Math.round()`.** Перша версія "бачить" використовувала
той самий множник `*0.15`, що й стіни, — і жодного разу не спрацювала
на живому тесті (0 приросту `VIS_LC` на дистанції 90px). Причина: стіни
використовують `closeness` аж до 1.0 (впритул), а зона "бачить" таргана
працює лише в діапазоні [55,120), де `closeness` ніколи не перевищує
~0.5 — `round(5*0.15*0.5)=round(0.375)=0`, тобто буквально мовчазний нуль
щотакту. Виправлено множником `*0.5` замість `*0.15` — перевірено живим
9-тактовим прогоном: `VIS_LC` стабільно росте (3→24) і зрештою реально
спрацьовує.

**Вибір "відповзти чи злетіти" — не сценарій, а наслідок того, що вже
вирішив конектом.** Нічого нового для самого рішення "летіти чи ні" не
писалось — той самий `escapeUrge`/`takeoff()` у `runBrainTick`, що вже
керує втечею від кліку/струсу/стін, спрацьовує автоматично, коли
`brain.flight` (акумулятор `DN_FLIGHT`, який тепер підживлює й тарган)
перетне поріг. Підтверджено живим тестом: дотик на дистанції 10px → вже
за 2 такти `mode` став `'air'` (реальний зліт). Єдине, що написано
окремо, — геометричний напрямок "геть від таргана" для випадку, коли
зліт НЕ стався (`ROACH_CLOSE_DIST` + той самий дебаунс-лічильник
`roachCloseStreak`, що й у `VIS_LC` вище — свідомо НЕ `brain.fear`, бо
рівні "бачить"/"близько" навмисно не піднімають скалярний страх напряму,
той самий принцип, що й у стін, задокументовано в розділі "Баг: не їсть,
коли голодна" вище; лише дотик додає його напряму). Підтверджено: 40
кадрів прогону з нерухомим тарганом за 40px — дистанція монотонно росте
(39.7→52.4px), `mode` лишається `'ground'`, `roachCloseStreak` коректно
наростає — справжнє "відповзання", а не просто прискорений випадковий
рух.

### Розкадровка таргана — та сама анімація для БУДЬ-якого напрямку через чисте обертання

Користувач надав 5-кадрову розкадровку ходи (top-down, "вусики вгору"),
разом із вказівкою "просто відзеркалюй чи крути". Використано ЧИСТЕ
обертання (без жодного дзеркалення) — для top-down вигляду це коректно
покриває всі 360°, дзеркалення не потрібне взагалі.

- Кадри вирізані по alpha-межах із наданого зображення (той самий метод,
  що й для `fly_climbing.png` цього сеансу раніше), кожен зі СВОЇМ
  виміряним центром (не єдиний спільний — той самий принцип, що й
  `groomFrame`/`climbFrames`: лапки на кожному кадрі ходи розташовані
  по-різному, спільний центр змусив би тіло "стрибати").
  Центр — не bbox-центр (він "гуляє" через асиметричний мах лапок,
  перевірено: розкид 33px по X), а alpha-зважений центроїд ТІЛЬКИ
  панцира (середні 45–65% висоти кадру, пікселі з R>70) — під нього
  підходить крапка приблизно на з'єднанні голови й тулуба, стабільно
  на всіх 5 кадрах (перевірено оверлеєм хрестиків — жодного видимого
  дрейфу).
- Вихідні кадри дивляться "вгору" (-90° у системі координат
  `Math.atan2(vy,vx)`), тому рендер додає `ROACH_SPRITE_FACING_OFFSET =
  Math.PI/2`, щоб `roach.angle=0` (вправо) відповідав фактичному
  напрямку спрайту.
- `ROACH_TARGET_H=24px` — навмисно менше за розмір мухи
  (`TARGET_BODY_H_WALK=45px`), за прямою вказівкою користувача.
- Кадр ходи гортається (`ROACH_STEP_MS=140мс`) лише під час руху
  (`'fast'`/`'slow'`); на `'still'` — стоп-кадр (лишається як є).

Перевірено: ізольований рендер на прозорому канвасі на 5 кутах
(0°/90°/180°/-90°/45°) — bbox коректно обертається (23×14 → 14×23 →
23×14 → 14×23 → 24×24 по діагоналі), кількість непрозорих пікселів
стабільна (145/145/145/145/150) — без артефактів чи асиметричного
обрізання; 20-хвилинна комплексна регресія з мухою, тарганом і всіма
рівнями зору/дотику одночасно — 0 помилок.

### Баг: "слабо реагує на їжу навіть на голоді 100%" — зона нюху застаріла після вільного повзання

Скарга користувача: "чомусь вона слабо реагує на їжу, коли голодна на
100%... може їй просто набридає чи не цікаво, чи вона звикла". Ніякого
звикання/нудьги в коді немає (і не малось бути) — знайдено справжній,
конкретний баг.

**Причина.** `anyFoodNearby()`/`findNearestFoodInRange()` вимагали
`zoneAt(f.x,f.y) === surfaceZone` — точного збігу зони їжі й зони, де
зараз муха. Коментар поруч буквально казав "запах не дозволяє їй
перелетіти зону, це так само вимагає польоту" — це було ПРАВДОЮ до
торішнього (цього ж сеансу) фіксу вільного повзання, коли будь-яка зміна
зони справді вимагала зльоту. Відколи стіл/підлога/стіна/бічна панель
(`GROUND_CRAWL_ZONES`) стали взаємно прохідними ПОВЗАННЯМ, цей чек
застарів: муха тепер регулярно опиняється на `'vertical'`/`'pcside'`
(і після фіксу проводить там ЗНАЧНО більше часу, ніж раніше), і рівно
там нюх повністю глухнув — незалежно від голоду чи радіуса.

Підтверджено живим тестом: голод 100% (радіус нюху 225px, максимум),
їжа за 15px, муха стоїть на `'pcside'` — `anyFoodNearby()` → `false`,
`findNearestFoodInRange()` → `null`. Це і виглядає ззовні як "не
цікавить їжа" — насправді вона її просто фізично не відчуває, хоча
дійти повзанням могла б за секунду.

**Виправлено:** рахуємо запах, якщо ОБИДВІ зони (де муха й де їжа) —
взаємно прохідні тим самим набором `GROUND_CRAWL_ZONES`, що й
`blocked()`. Не змінено нічого в самій логіці підходу/поїдання — щойно
`smelledFood` перестав бути `null`, решта (наближення, `tryMoveGround()`,
`syncSurfaceZone()`) уже працювала коректно.

Перевірено: той самий сценарій (100% голоду, 15px, `'pcside'`) — тепер
`anyFoodNearby()===true`, і живий прогін показує реальне поїдання за
~6с з того самого місця; дві незалежні 20-хвилинні регресії (старт з
голодом 50% і зі 100%, з тарганом, з періодичними "Кинути їжу") — обидві
показують здорове регулярне коливання голоду 0↔100% (десятки поїдань),
0 помилок консолі.

**Побічна знахідка під час тестування (не гри, а методології):**
перевикористання `let`-таймерів (`lastHungerTick`, `lastBrainTick`,
`nextPoopAt`) між ОКРЕМИМИ тестовими прогонами на тій самій сторінці без
скидання — коли синтетичний `tNow` одного прогону "втік" далеко в
майбутнє, а наступний прогін почав рахувати `hDt` від НОВОГО реального
`performance.now()`, різниця виходила катастрофічно від'ємною (голод
уходив у -1300). Причина суто в способі тестування (заморожений rAF +
перевикористання сторінки), не в грі; з тих пір перед кожним новим
синтетичним прогоном явно скидаю ці таймери до свіжого `tNow`.

### Оголений провід — розміщення спрайту (логіка описана нижче, у розділі про навчання)

За прямою вказівкою користувача ("спочатку розмісти, потім я перевірю і
перейдемо далі до логіки") — цього разу ЛИШЕ розміщення `electricity.png`
поверх кабелю клавіатури, жодної небезпеки/логіки ще не підключено.

**Позицію визначено не на око.** Користувач надав референс-скріншот
(мокап з іскрою на проводі). Замість того щоб на око прикидати координати,
зіставив кольорові орієнтири — 4 кольорові кола іконки на екрані монітора
(червоне/фіолетове/бірюзове/зелене) — між референсом і фактичним фоном
гри (`desk.jpeg`): знайшов alpha-зважений центроїд кожного кольору в
ОБОХ зображеннях (3 незалежні пари точок), розв'язав афінне
перетворення (масштаб+зсув, без обертання — 3 точки дають точний
розв'язок), і застосував його до позиції іскри в референсі, щоб
отримати її справжні координати на канвасі гри. Отримана точка звірена
з реальним вигином кабелю на дрібній (5px) сітці — збіглася з точністю
до кількох пікселів.

**Кут спрайту (~45°, згори-зліва вниз-праворуч) вже збігається з
нахилом кабелю САМЕ в цій точці вигину** — перевірено порівнянням
власної діагоналі спрайту (виміряно на збільшеному зображенні) з
локальним напрямком кривої кабелю в desk.jpeg. Обертання не
знадобилось.

**Масштаб:** довжина графіки проводу в референсі (~164px у координатах
референс-зображення) переведена через той самий афінний масштаб
(~5.2×) у desk.jpeg-масштаб (~31.5px) — це дало коефіцієнт зменшення
спрайту ×0.33 від оригінального розміру 86×77, звірено з фактичною
товщиною намальованого кабелю (~6–8px) — збігається.

Малюється одразу після фону (`ctx.drawImage(desk...)`), до дебаг-оверлею
зон — фіксована декорація сцени, координати `ctx.drawImage(electricityImg,
116, 331, 28, 25)`.

Перевірено: живий рендер, приближений кроп канвасу через `getImageData` —
іскра лягає точно на вигин кабелю, без зсуву; 0 помилок консолі.

### Справжнє навчання (плюсність): чашка + оголений провід + кавова пляма

Реалізовано перші два пункти узгодженого плану навчання (мемо
`fly-learning-roadmap`: чашка → провід → тарган-востаннє, лише після
розділення джерел `VIS_LC`). Тарган НЕ чіпався цього разу — за прямою
вказівкою користувача з попереднього ходу ("Таргана — одразу після того,
як патерн на чашці підтвердиться"), навіть попри те, що це наступне
повідомлення користувача знову згадало "чашка, тарган, електрика" разом —
нагадав про це в чаті, як він і просив, і зробив лише чашку+провід+каву.

**Чому спільний `MB_KC` не годиться для навчання per-stimulus.**
`MB_KC` (Kenyon cells) у цьому конектомі — ОДИН лумпований вузол на весь
нюх/зір/смак одразу (справжня муха кодує запах комбінаторним патерном по
~2000 реальних Kenyon-клітин — sparse coding, якого тут немає, лише ~70
груп). Якби пластичність мінялась напряму на вазі `MB_KC→MB_MBON_AV`,
"навчений страх" від чашки просочився б у геть усе, що коли-небудь
торкається `MB_KC` (їжа, запахи, кольори) — попри те що боятись реально
повинна лише самої чашки. Рішення (саме те, що описане в
fly-learning-roadmap): для кожного подразника, що навчається окремо, —
своя маленька "клітина" вручну: `MB_KC_HOT` (чашка) і `MB_KC_WIRE`
(провід), кожна зі своєю окремою вихідною вагою до `MB_MBON_AV`.

**Де сходяться CS (умовний стимул) і US (безумовний, покарання) —
`MB_DAN_PUN`.** Реальна нейронаука Drosophila (Cohn/Aso/Owald, кластер
дофамінових нейронів PPL1): РІЗНІ аверсивні модальності (біль/шок, гіркий
смак, спека) конвергують на ОДНОМУ дофаміновому "punishment"-хабі, а не
йдуть окремими каналами. Тому додав `MB_DAN_PUN` як ціль і в `NOCI` (біль,
вага 5), і в новий `THERMO_MUG` (вага 4) — так само, як він уже раніше йшов
від `GUS_GRN_BITTER` (вага 7, існувало до цього ходу).

**`THERMO_MUG` — чому не просто перевикористати `THERMO_WARM`.**
`THERMO_WARM` реагує на ВСЕ тепле в сцені (верх ПК, верх монітора, вікно
вдень, чашка вдень) — якби навчання чіплялось до нього, страх від чашки
узагальнився б на звичайне тепло електроніки. Тому `THERMO_MUG` — окремий
вузол, що стимулюється (`senseMug()`, викликається з `runBrainTick` у
блоці `mode==='ground'`) ЛИШЕ від `HEAT_SOURCE` (виміряний центр/радіус
САМЕ чашки з `computeHeatSource()`), і лише коли вона зараз реально
гаряча (вдень; вночі кава вихолола, сигналу нема). `THERMO_MUG` має дві
вихідні вазі: `MB_KC_HOT` (CS, впізнавання) і `MB_DAN_PUN` (US, майже
опік сам собою).

**Провід — на відміну від чашки, нуль вродженої аверсії.** У чашки
фізична зона `'hot'` вже непрохідна ще до будь-якого навчання (тому
`MB_KC_HOT` стартує з невеликою вродженою вагою 2 — вона й так гаряча).
Провід — новий об'єкт без жодної фізичної перешкоди в растрі зон: муха
може дійти впритул і реально торкнутись. Класична павловська схема
(Quinn/Tully, оригінальний shock-avoidance парадигм Drosophila):
нейтральний стимул + шок → навчений страх. Тому `MB_KC_WIRE` стартує з
0 — жодної вродженої аверсії, вся вона навчена.

`senseWire()` (викликається поруч із `senseMug()`) — дві фази:
- **SEE** (`WIRE_SEE_DIST=50px`): `VIS_WIRE` (CS, зорове впізнавання іскри),
  масштабовано зростає з наближенням — той самий патерн, що й
  `senseRoach()`'s SEE-рівень.
- **TOUCH** (`WIRE_TOUCH_DIST=11px`, з кулдауном 4с — `WIRE_SHOCK_COOLDOWN_MS`):
  реальний контакт. Пряме (нескальоване) `dendriteAccumulate` на `NOCI`,
  `DN_STARTLE` і `VIS_WIRE` одночасно — той самий miттєвий "стоп-кадр"
  такту, що й TOUCH-рівень таргана (усе розв'яжеться разом у
  `BRAIN.update()` цього ж такту), плюс `BRAIN.stimulate.touch=true` —
  повторно задіює той самий дотиковий шлях, що й дотик таргана.
  `WIRE_X=130, WIRE_Y=343.5` — точний центр раніше розміщеного спрайту
  (`drawImage(electricityImg,116,331,28,25)` → 116+28/2, 331+25/2).

**Сама пластичність — `applyMBPlasticity()`, викликається в `runBrainTick`
одразу після `BRAIN.update()`.** Модель — спрощений слід придатності
(eligibility trace), 3-факторне правило (Hebbian × дофамін), стандартне
для MB-літератури й для класичного RL (Sutton & Barto):

1. Якщо `MB_KC_HOT`/`MB_KC_WIRE` спрацював цього такту (є в
   `BRAIN.firedThisTick`) — його "слід" (`kcHotTrace`/`kcWireTrace`)
   встановлюється на `MB_PLASTICITY_TRACE_TICKS=3` такти (~1.5с — той
   самий дебаунс-принцип, що й `VISION_LOOM_STREAK_NEEDED`), інакше
   згасає на 1 за такт.
2. Якщо `MB_DAN_PUN` спрацював ЦЬОГО такту, і слід ще активний — вага
   `weights.MB_KC_HOT.MB_MBON_AV` (або `MB_KC_WIRE`) РЕАЛЬНО зростає на
   `MB_PLASTICITY_RATE=0.6` (за раз), до стелі `MB_PLASTICITY_CEILING=8`
   (не вища за вроджені ваги на кшталт `NOCI→DN_STARTLE:10`, щоб не
   зламати баланс конектома).
3. Щотакту, незалежно від (1)-(2), обидві ваги повільно тягнуться назад
   до своєї вродженої бази (`MB_PLASTICITY_DECAY=0.02`/такт) — забування
   без підкріплення, так само як реальна MB-пам'ять згасає з часом.

Це відрізняється від фейкової "пластичності", задокументованої раніше
(коментарі `MB_DAN_REW/MB_DAN_PUN → MB_KC:3` кажуть "reward/punishment-
gated plasticity", але то фіксоване число, яке ніколи не росте) — тут
вага в самому об'єкті `weights` СПРАВДІ змінюється в рантаймі й одразу
впливає на наступні `dendriteAccumulate` виклики.

**Downstream: чому не знадобилось окремої "тікай від чашки" логіки.**
`MB_MBON_AV` уже й так штовхає `DRIVE_FEAR`/`DN_STARTLE`/`LH_AV` (існуючі
ваги). Коли `MB_KC_HOT→MB_MBON_AV` зростає з навчанням, та сама
близькість до чашки дає СИЛЬНІШИЙ поштовх у `DRIVE_FEAR` — і вже наявна
поведінка (той самий каскад, що й для стін/таргана — "чому вона тікає з
fear:0") природно відреагує раніше й сильніше. Жодного нового
steering-коду не знадобилось для чашки. Для проводу є явний форсований
зліт при контакті (`takeoff()` одразу після NOCI/startle, той самий
патерн, що й `shakeTable()`), бо реальний удар струмом — це миттєвий
рефлекс, не поступове накопичення.

**Перевірено живими синтетичними тестами (headless, з ручним драйвом
конектома, без покладання на скриншоти):**
- Ізольований тест лише проводу (`senseWire()` + `BRAIN.update()` +
  `applyMBPlasticity()`, 8 тактів поспіль, кулдаун ігнорується навмисно):
  на 5-му такті `MB_DAN_PUN` і `MB_KC_WIRE` спрацювали В ОДИН ТАКТ — вага
  стрибнула 0 → 0.58; наступні такти без збігу — плавно згасає
  0.58→0.56→0.54→0.52. Точно відповідає описаній моделі.
- Живий 50-тактовий тест зі свободним блуканням біля чашки (без
  форсування) — вага `MB_KC_HOT.MB_MBON_AV` природно виросла з вродженої
  бази 2.0 до 2.98 просто від того, що муха проходила повз/затримувалась
  біля чашки під час звичайної поведінки.
- 10-хвилинний випадковий регресійний прогін (1200 тактів, 6 трусів
  столу) — 0 помилок консолі, жодного NaN у ключових змінних (hunger,
  pos, dopamine, stress, обидві навчені ваги).

### Кавова пляма — вода з кофеїном, перша реальна активація `GUS_GRN_WATER`

За прямою вказівкою користувача: з'являється, коли труснути стіл (і
слабко, і сильно — жодної різниці), прибирається кнопкою "Очистити",
це джерело ВОДИ (не їжі), але з кофеїном.

**Звідки береться пляма.** `spawnCoffeeStain()` (викликається з
`shakeTable()`) розхлюпує каву з ТІЄЇ Ж чашки — шукає спіраллю навколо
виміряного `HEAT_SOURCE` (центр чашки) найближчу клітинку, куди муха
реально може доповзти (`'walk'`/`'floor'`), СТАРТУЮЧИ з радіуса 45px, не
з краю самої зони `'hot'`. Перша спроба з радіусом 18px лягала прямо на
ручку чашки (зона `'hot'` покриває лише ~20×20px "устя", а не весь
видимий керамічний корпус+ручку) — перевірено живим скріншотом, виправлено
збільшенням стартового радіуса; повторно перевірено — пляма тепер лягає
чисто на дерево столу, поруч із чашкою, не перекриваючи її малюнок.
Лише ОДНА пляма за раз (`foods.some(f=>f.type==='coffee')` перевіряє
перед спавном); "Очистити" вже й так скидає `foods=[]`, що прибирає й
пляму — вона просто ЗВИЧАЙНИЙ елемент того самого масиву `foods`, з
`type:'coffee'`, жодної окремої структури даних не знадобилось.

**Спрайт.** `spilled coffe.png` користувача, обрізано до alpha bbox
(146×78, з 156×104), вбудовано як є (без ресемплу, як і `electricity.png`
раніше) — розмір на канвасі підібрано скромним (`COFFEE_DISPLAY_W/H` =
30×16), бо сама зона `'hot'` (чашка) — лише ~20×20px, більша пляма
виглядала б непропорційно.

**Смак — чесний змішаний, не вигаданий окремий шлях.** Кава — це і вода
(`GUS_GRN_WATER`, реально задіяний ВПЕРШЕ — раніше існував у вагах, але
ніколи не стимулювався жодним кодом гри, точно як дотик таргана до
цього ходу), і трохи гіркоти від кофеїну (`GUS_GRN_BITTER`, приглушена
`×0.4` — п'є її, а не уникає, але це вже не чиста нагорода, як солодке).
Новий прапорець `BRAIN.stimulate.waterContact` (поруч із `foodContact`)
у головному стимул-блоці штовхає обидва нейрони одночасно.

**Чому голод падає лише частково, а не до нуля.** На відміну від
справжньої їжі (`hunger=0`, повна "харчова кома"), кава дає лише
`COFFEE_HUNGER_RELIEF=15` — це вода, не поживна речовина; фазова
модель ситості (`satietyPhase` кома→перетравлення→гострий голод) не
чіпається зовсім, голод одразу продовжує йти своєю звичайною фазою.
Це свідоме спрощення: повноцінної окремої моделі спраги (`DRIVE_THIRST`
у конектомі не існує) поки немає — користувач сам зазначив, що "згодом
додамо й джерело чистої води", тобто повна спрага — наступний крок,
не цей.

**Кофеїн — реальна фармакологія, і пряме використання для навчання
(як і питав користувач).** `caffeineUntil` (25с від моменту випивання,
`CAFFEINE_DURATION_MS`) дає три ефекти, усі увімкнені лише поки діє:
1. `groundSpeed()`: `×CAFFEINE_SPEED_MULT=1.25` — вища рухливість.
2. `runBrainTick`: `BRAIN.drives.fatigue` щотакту трохи знижується
   (`CAFFEINE_FATIGUE_RELIEF_PER_TICK=0.03`) — реальна фармакологія
   кофеїну (антагоніст аденозинових рецепторів → менше відчуття втоми),
   не вигаданий "бафф".
3. **`applyMBPlasticity()`: швидкість навчання (`MB_PLASTICITY_RATE`)
   масштабується `×CAFFEINE_PLASTICITY_MULT=1.5`, поки кофеїн діє.** Це
   пряма відповідь на власне запитання користувача "якщо кофеїн впливає
   на муху, це можна використати для навчання" — кофеїн підсилює
   дофамінову сигналізацію в реальній фармакології, тут це буквально
   прискорює той самий дофамін-гейтований механізм із розділу вище.

**Перевірено:** прямий тест гілки поїдання (`state='atfood'`,
`atFoodTarget`=пляма, forced `feedReady`) — `hunger` впав з 90 до ~75.7
(≈15, як і задумано), `caffeineUntil` встановився в майбутнє
(`caffeineActive:true`), `dopamine` реально зросло (22.5→32.2, від
справжнього `MB_DAN_REW`), стан коректно повернувся в `'walk'` з
очищеним `atFoodTarget`. Регресія: звичайна їжа (салямі) після цих змін
досі скидає `hunger` в 0 і `satietyPhase` в `'coma'` як і раніше —
шлях кави й шлях їжі не заважають один одному.

## Що НЕ реалізовано (свідомо відкладено)

- Реальне "поїдання" їжі (їжа не зникає після `atfood`, лише хунгер-статистика
  й нагорода з `MB_DAN_REW` оновлюються)
- Справжня фізика польоту (гравітація/втома — стилізовані, не SI-одиниці)
- planck.js фізика тіла з 6 ногами (була в оригінальному плані архітектури,
  ще не почата)
- Повна 139k-нейронна модель flybrain через Web Worker (див. вище чому)
- Навчання таргана (`MB_KC_ROACH`) — за прямою вказівкою користувача,
  свідомо востаннє в черзі: лише ПІСЛЯ підтвердження, що патерн навчання
  на чашці/проводі справді працює довше, І після розділення джерел
  `VIS_LC` (наразі стіни/таргана/клік усі штовхають той самий спільний
  нейрон — без розділення навчений страх від таргана хибно узагальнився б
  на звичайні стіни). Див. пам'ять `fly-learning-roadmap`.
- Повноцінна модель спраги (`DRIVE_THIRST`, окремий індикатор на кшталт
  голоду) — кавова пляма поки лише частково впливає на `hunger`
  (`COFFEE_HUNGER_RELIEF`), справжня спрага з'явиться разом із майбутнім
  джерелом чистої води (сам користувач це відклав: "згодом додамо й
  джерело чистої води")
